# AcouDiet（声膳）项目实现计划方案（工程执行版）

**版本**：v3.4（团队 **3 人**；**更名 AcouDiet / 声膳 已全量执行**；环境已就绪；已执行范围冻结；全部修正已贯穿全文）
**编制依据**：`D:\Desktop\Food` 文件夹全部材料（2 份项目计划书 docx、7 份讨论与建议文本、10 张 UI 设计图、5 张架构与功能期望图）
**配套文档**：`AcouDiet_更名执行包.md`（查重记录、全局替换清单、Related Work 成稿、答辩话术）
**性质**：这是"可执行 + 可答辩 + 抗追问"的实施方案，不是概念包装稿。凡与原始材料冲突之处，均标注**冲突编号**并给出处置结论。

> ℹ️ **关于本文档中的 `EatSense` / `ChewSense` 字样**：这两个名称均已废弃（§3.0）。**更名已于 2026-09-10 全量执行完毕**（7 份 txt + 2 份 docx，详见 §3.0.4）。本文档中残留的旧名**仅用于指代历史材料与查重记录**，属刻意保留的可追溯性内容，**不要再次替换**。

---

## 0. 给决策者的 60 秒摘要

| 项目 | 结论 |
|---|---|
| 方案核心是否成立 | **成立**。声学无感饮食感知是真实学术方向，交互闭环完整，Demo 故事性强 |
| 原始方案最大问题 | **不是 AI 不行，而是关键参数自相矛盾、UI 与模型能力严重不匹配**（详见 §3） |
| 最高优先级修复 | ①修 Mel 时间窗数学错误 ②TFLite 转换改走 TF 路线 ③砍掉卡路里/营养素主张 ④改掉命名冲突 |
| 🔴 阻塞级发现 | **"EatSense" 已被 2023 年爱丁堡大学学术数据集占用**，必须更名（详见 §3.0） |
| 本机能否开工 | **不能直接开工**。缺 Flutter/Android SDK，Python 环境为 3.7 + CPU-only torch，RTX 5060 完全用不上（详见 §2） |
| **团队规模** | **3 人（已确认）** |
| 10 天可行性（3 人） | ⚠️ **按原功能清单不可行**（超载约 35%）。**必须先执行 §8.2 的范围冻结**，砍掉 7 项非关键功能后才可交付 |
| 3 人版最大的单点风险 | **B 一人承担全部原生音频 + Mel 前端 + Flutter 集成 + Demo 模式**，是全队唯一不可替代的瓶颈（详见 §8.4） |
| 推荐定位 | 保留声学识别为技术亮点，把"**无感记录 + 行为分析**"作为主张核心 |

---

## 1. 材料清点与需求归并

### 1.1 素材清单与作用

| 文件 | 性质 | 在计划中的用途 |
|---|---|---|
| `AcouDiet_项目计划书_1版_已更名.docx` | 项目计划书 v1（819 行，18 章） | **技术权威来源**。含完整模块设计、超参、数据规范、API 字段表 |
| `AcouDiet_项目计划书_2版_已更名.docx` | 项目计划书 v2（800 行） | v1 的收敛版。仅 §2.2、§3.1 有实质改写，其余**逐字相同** |
| `讨论1.txt` | 可行性审查 | 风险清单、答辩 Q&A、10 天节奏建议 |
| `讨论2.txt` | 竞赛导向方案 | 8 类降级策略、三模式 Demo、创新点包装 |
| `讨论3.txt` | 优点与质疑点 | 类别难度分级、噪声问题 |
| `讨论4.txt` | 最终竞赛版 | 双任务头、自采数据 1000 条、10 天表 |
| `执行规划草稿.txt` | 执行版 | 6 类冻结点、开发目录、验收标准、降级路径 |
| `导师汇报部分参考.txt` | PI 汇报稿 | 汇报结构、待验证问题清单 |
| `讨论的修改建议汇总.txt` | **最高价值文件** | 健康评分算法、按人划分数据集、指标要求 |
| `APP期望功能汇总/1.jpg` (6 段长图) | 架构图 + 竞赛策略 | **最终架构权威版本**、Slogan、宣传口径红线 |
| `APP期望功能汇总/2.jpg` | 架构图 | 同上（单页版） |
| `APP期望功能汇总/3-5.jpg` | 四大页面功能定义 | 首页/AI检测/饮食记录 逐项功能规格 |
| `软件UI界面设计图/1-5.png` | UI 视觉稿 | **视觉验收基线** |
| `软件UI界面设计图/6-10.png` | 1-5.png 的重命名副本 | 经 SHA-256 比对确认为同一文件（6=1, 7=2, 8=4, 9=5, 10=3） |

> **发现 N-1（材料冗余）**：`软件UI界面设计图` 的 10 张图实际只有 5 张独立设计稿，其余为重复副本。建议整理归档，避免向评委提交重复素材。

### 1.2 提取出的最终架构（来自 `APP期望功能汇总/1.jpg`、`2.jpg`）

这是材料中**最新的架构定版**，优先于 docx 中的旧架构图：

```
AcouDiet
  └─ 手机麦克风采集
      └─ 音频预处理（16kHz / 分帧 / 降噪）
          └─ Mel Spectrogram
              └─ MobileNetV3-Small
                  └─ 食物类别识别
                      ├───────────────────────┐
                      ↓                       ↓
                 食物知识库                声学行为分析
                 食物属性/类别           咀嚼次数 / 节律
                                        进食时长 / 进食速度
                      └───────────┬───────────┘
                                  ↓
                          饮食行为数据库
                                  ├──────────┐
                                  ↓          ↓
                              饮食日志    健康分析
                                             ↓
                                      个性化建议
```

**关键差异（相对 docx v1）**：食物属性不再由模型第二个输出头产生，而是由**食物类别 → 食物知识库 → 属性**的查表推导。这与 `讨论的修改建议汇总.txt` 建议(2) 完全一致，**本方案采纳并冻结**。

### 1.3 目标平台冲突（必须裁决）

| 来源 | 平台主张 |
|---|---|
| docx v1/v2 §9.1、§16.1 | Flutter + Dart，**Android 与 iOS 双平台** |
| `导师汇报部分参考.txt` 末段 | 明确告知导师"采用 **Flutter 跨平台**开发" |
| `APP期望功能汇总/1_p5.jpg` | 核心技术写"**Android 端侧推理**" |
| `讨论2.txt` | "移动开发比赛 **Android 原生更加稳**" |
| `讨论4.txt` §8 | Android + Kotlin + TFLite |

> **冲突 C-1**：双平台 vs Android 单平台。
> **裁决**：**Flutter 框架保留**（已向导师承诺，且 5 页面 UI 用 Flutter 开发效率最高），但 **v1.0 只发布并验证 Android APK**。iOS 从"交付物"降级为"架构已兼容、未验证"。
> **理由**：TFLite 的 Hardswish/SE 算子在 Core ML 转换链上失败率高；10 天窗口内不可能同时调通两端；评委不会因双平台加分，但会因 iOS 现场崩溃扣分。

---

## 2. 环境与技能缺口（对应"缺失技能则自行安装"）

### 2.1 本机现状实测结果

| 依赖 | 状态 | 影响 |
|---|---|---|
| Python | **3.7.0** (`D:\Anaconda`) | 🔴 过旧。librosa 新版本、PyTorch 2.x CUDA 均不支持 |
| PyTorch | 1.13.1 **+cpu** | 🔴 无 CUDA。且 1.13 不支持 sm_120 架构 |
| `torch.cuda.is_available()` | **False** | 🔴 GPU 完全不可用 |
| GPU | RTX 5060 Laptop 8GB | ✅ 硬件在，**软件栈用不上**（Blackwell sm_120 需 CUDA 12.8+ / PyTorch 2.7+） |
| librosa / soundfile | **缺失** | 🔴 音频处理链断了 |
| tensorflow | **缺失** | 🔴 无法导出 TFLite |
| onnx / onnxruntime | **缺失** | 🔴 无法走转换链 |
| scikit-learn | 0.19.2 | 🔴 极旧，混淆矩阵 API 不兼容 |
| Python 3.11+ | 缺失 | 🔴 需新建环境 |
| **Flutter SDK** | **缺失** | 🔴 移动端无法开工 |
| **Dart** | **缺失** | 🔴 同上 |
| **Android SDK / adb / Gradle** | **缺失** | 🔴 无法编译 APK、无法真机调试 |
| Java | ✅ 已装 | ⚠️ 需确认是 JDK 17（Flutter 要求），Oracle javapath 通常是 JRE |
| conda / pip / npm / pnpm / git | ✅ 可用 | ✅ 具备自动化安装能力 |

> **冲突 C-2**：docx §13 的 D1 是"Flutter 框架搭建"，但**本机 Flutter 尚未安装**。原计划把环境准备计为 0 成本，实际至少消耗 0.5–1 天。
> **裁决**：新增 **D0（并行准备日）**，AI 组与 App 组各自独立装环境，不占用 D1–D10 主线。

### 2.2 安装方案（按优先级）

#### A. AI 训练环境（高优先级，D0 必须完成）

```powershell
# 1) 安装 Miniconda 后创建独立环境（不要污染现有 Anaconda base）
conda create -n eatsense python=3.11 -y
conda activate eatsense

# 2) PyTorch CUDA 版（CUDA 12.8，支持 Blackwell sm_120）
pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu128

# 3) 音频与训练依赖
pip install librosa==0.10.2.post1 soundfile audiomentations pyloudnorm
pip install numpy scipy scikit-learn pandas matplotlib seaborn tqdm
pip install torchmetrics            # 混淆矩阵 / F1 / 宏平均指标
pip install tensorflow==2.17.0      # 仅用于导出 TFLite（见 §5.4 决策）
pip install onnx onnxruntime        # 备用转换路径
pip install kaggle                  # 数据集下载
```

**验证脚本（D0 验收，必须全绿）**：

```python
import torch, librosa, soundfile, sklearn, tensorflow as tf
assert torch.cuda.is_available(), "CUDA 未就绪"
print(torch.cuda.get_device_name(0), torch.version.cuda)
print("librosa", librosa.__version__, "| tf", tf.__version__, "| sk", sklearn.__version__)
# 最小端到端冒烟：WAV -> Mel -> tensor
y, sr = librosa.load(librosa.ex("trumpet"), sr=16000, mono=True)
m = librosa.feature.melspectrogram(y=y, sr=16000, n_fft=1024,
                                   hop_length=512, n_mels=128, fmin=20, fmax=8000)
print("mel shape:", m.shape, "-> log:", librosa.power_to_db(m).shape)
```

> ⚠️ **风险 R-ENV-1**：RTX 5060 Laptop 8GB 的 **8GB 显存对本任务完全够用**（MobileNetV3-Small + batch 32 + 128×128 输入，显存占用 < 1.5GB）。真正的瓶颈是 CUDA 版本匹配，不是显存。
> ⚠️ **风险 R-ENV-2**：若 CUDA 环境安装不顺利，**不要卡在这里**。CPU 训练 MobileNetV3-Small 在 11k 片段规模下约 40–90 分钟/50 epoch，**完全可接受**。设定 2 小时止损线，超时立即切 CPU 路线。

#### B. 移动端环境（D0 并行，D1 必须可用）

```powershell
# 1) Flutter SDK（建议 3.24.x stable）
git clone -b 3.24.5 https://github.com/flutter/flutter.git D:\dev\flutter
# 将 D:\dev\flutter\bin 加入 PATH

# 2) 安装 JDK 17（Flutter/AGP 要求，Oracle javapath 通常是 JRE 不够用）
#    下载 Temurin JDK 17 并设 JAVA_HOME

# 3) Android 命令行工具 + SDK
#    下载 commandline-tools，然后：
sdkmanager "platform-tools" "platforms;android-34" "build-tools;34.0.0" "ndk;26.1.10909125"

# 4) 验证
flutter doctor -v          # 必须 Android toolchain 全绿
flutter config --android-sdk <SDK路径>
```

#### C. 选装（有则加分，无则跳过）

| 工具 | 用途 | 优先级 |
|---|---|---|
| `audiomentations` | 音频增强库，省去手写增强 | 高 |
| `pyloudnorm` | LUFS 响度标准化 | 中 |
| FFmpeg | 音频格式批量转换 | 中 |
| `onnx2tf` | ONNX→TF 备用转换路径 | 低（仅当 TF 直训路线受阻） |
| Android Studio | 可视化调试 UI / Profiler | 中（可用 VS Code 替代） |

### 2.3 缺失"技能"清单（人的能力，非软件）

| 技能 | 谁需要 | 缺口程度 | 补齐方式 |
|---|---|---|---|
| Flutter 基础 + 状态管理 | App 组 | 高 | 官方 codelab + 本项目 5 页面模板，D0–D1 完成 |
| TFLite 集成 | App 组 | 中 | `tflite_flutter` 官方 example 直接改 |
| Kotlin 原生音频（AudioRecord） | App 组 | **高** | 关键路径，必须 D0 就啃（见 §5.3） |
| Mel 频谱原理与参数一致性 | AI 组 + App 组**双方** | **高** | 共享同一份 `feature_config.json`（见 §5.3） |
| 音频数据增强 | AI 组 | 中 | `audiomentations` 文档 |
| 混淆矩阵/置信区间报告 | AI 组 | 中 | `torchmetrics` + Wilson 区间公式 |
| 数据采集伦理与知情同意 | 产品组 | 中 | 见 §7.4，模板已给 |

---

## 3. 漏洞与不足清单（评审结论）

> 分级：🔴 阻塞级（不修就出事） / 🟠 高风险（会被评委追问穿） / 🟡 中风险（影响质量） / 🔵 优化项

### 3.0 🔴 漏洞 1：项目命名与领域内已有工作**双重冲突**（比预想严重）

#### 3.0.1 冲突一：同名数据集

`EatSense` 已被 2023 年发表的数据集占用 —— Raza, M. A., Chen, L., Li, N., & Fisher, R. B. (2023), *"EatSense: Human Centric, Action Recognition and Localization Dataset for Understanding Eating Behaviors and Quality of Motion Assessment"*，爱丁堡大学（[University of Edinburgh Research Portal](https://www.research.ed.ac.uk/en/datasets/eatsense-human-centric-action-recognition-and-localization-datase/)、[ACM DL](https://dl.acm.org/doi/full/10.1145/3774906.3802763)、[CORE 全文](https://core.ac.uk/download/620947325.pdf)）。做的正是**饮食行为识别**，领域完全重叠。

#### 3.0.2 冲突二 ⚠️ **我上一版推荐的 `ChewSense` 同样不可用，且是直接竞品**

**更正声明**：本方案 v3.0 曾推荐更名 "ChewSense"，**该推荐已作废**。尽调后发现：

> **ChewSense: Real-Time Detection of Chewing Counts and Food Types with Reverse Signals from Headphones** —— 与本站项目**做的是同一件事**（咀嚼计数 + 食物类别），且已发表、有开源数据采集代码库。
> 来源：[Mendeley 收录](https://www.mendeley.com/catalogue/1c21c88b-41c7-30a6-8acb-52b28fab1ed5/)、[项目主页](https://zacharysturman.com/projects/chewsense)、[GitHub 数据采集与标注仓库](https://github.com/ZSturman/ChewSense---Data-Collection-and-Labeling/blob/main/README.md)、[技术复盘文](https://dev.to/zacharysturman/building-chewsense-using-motion-data-for-an-on-device-ai-health-agent-57d5)

**教训（写进团队规范）**：**命名必须做查重，不能凭感觉推荐**。查重范围至少覆盖：Google Scholar / ACM DL / IEEE Xplore / GitHub / Google Play / App Store。

#### 3.0.3 含义反转为好消息：你们的差异化主张被验证了

原方案（`讨论1.txt` Q2）的答辩口径是：

> "现有研究主要集中在**可穿戴设备**和**实验环境**，而我们的目标是在**普通手机硬件**条件下实现低成本部署。"

**这个说法是对的，而且现在有硬证据。** 该方向近年的代表性工作**全部依赖耳戴设备**：

| 工作 | 年份 | 采集硬件 | 任务 |
|---|---|---|---|
| [AutoDietary](https://ieeexplore.ieee.org/document/7206521/similar) | 2016 | 颈挂式麦克风 | 食物摄入识别 |
| [ChewSense](https://www.mendeley.com/catalogue/1c21c88b-41c7-30a6-8acb-52b28fab1ed5/) | 2025 | **耳机**（反向信号） | 咀嚼计数 + 食物类别 |
| [NutriEar](https://dl.acm.org/doi/full/10.1145/3774906.3802763) | 2025 | **入耳式**麦克风 | 营养感知食物分类 |
| [AudiNosh](https://www.semanticscholar.org/paper/AudiNosh%3A-Seamless-Dietary-Behaviors-Optimization-Ahmed-Yang/e4464405d3fddb67134c64bf243912944fd3bb54) | 2025 | **入耳式**声学检测 | 饮食行为优化 |
| [Eat2pic-Mobile](https://dl.acm.org/doi/10.1007/978-3-031-92980-9_22) | 2025 | 手机 + 耳戴设备 | 咀嚼引导交互 |

**结论**：
- ✅ **差异化成立**：**"零额外硬件 / 仅用手机自带麦克风"是一个真实且未被占据的定位。**
- ⚠️ **但表述必须精确**："几乎没有研究"是**错的**（会被打脸），"现有工作全部依赖耳戴或专用硬件，我们只用手机"是**对的且可举证**。
- 🔴 **必须补 Related Work**：上表 5 篇是答辩的**必备弹药**，不是可选装饰。

#### 3.0.4 处置：更名决策（**✅ 已拍板：AcouDiet / 声膳**）

> **决策已定**：项目更名为 **AcouDiet**（中文：**声膳**）。
> **完整执行方案见独立文档 `AcouDiet_更名执行包.md`**（含查重记录、全局替换清单、Related Work 成稿、答辩话术）。
> 本节仅保留决策依据与本方案内需同步的要点。

**选定名称**：**AcouDiet** = Acoustic + Diet ｜ 中文 **声膳**（"声"= 声学感知，"膳"= 膳食管理）

**查重结果**：通用 Web / 学术库 / GitHub / 应用商店 / 近似词（`AkouDiet`/`AcuDiet`/`Acou-Diet`）**均无命中**；中文"声膳"仅命中古诗文用典，无产品、无商标。

**⚠️ 唯一阻塞项**：搜索引擎有覆盖盲区。**交付前必须由成员亲自在 5 处人工复核并截图存档**（Google Scholar/知网、ACM DL/IEEE、GitHub、Google Play/App Store、中国商标网）。**5 项全绿才可推进**；任一处有同名 → 启用备选 DineSense。

**被否决的候选（留档，防止再踩）**：

| 候选 | 否决原因 |
|---|---|
| ~~ChewSense~~ | **已被竞品占用**且是直接竞品（咀嚼计数 + 食物识别，2025） |
| ~~EatSense~~ | 已被 2023 年爱丁堡大学数据集占用 |
| ~~FoodSense~~ | 已有同名 GitHub 项目 |

**本方案内需同步的替换点**：

| 位置 | 处理 | 状态 |
|---|---|---|
| 本文档标题与全文 | → `AcouDiet`；文件名已改为 `AcouDiet_项目实现计划方案_v3.md` | ✅ **已完成** |
| 7 份 txt 讨论稿 | 全量替换（共 15 处） | ✅ **已完成** |
| 2 份 docx 计划书 | 生成已更名副本 `AcouDiet_项目计划书_{1,2}版_已更名.docx`（各 23 段） | ✅ **已完成** |
| 重复副本 `6~10.png` | 删除冗余｜UI 稿 Logo 替换 | ⏸ D1 随 UI 改造一并做 |
| 代码包名 / applicationId / 类名前缀 | 代码尚未创建 | ⏸ D1 建工程时直接使用新名 |

> ⚠️ **遗留**：原始 docx 文件（`AcouDiet——基于端侧AI声学感知…1版.docx` / `2版.docx`）是**只读文件**，本环境无法覆写。其**内部仍含 23 处 `EatSense`**。
> **处置**：使用已生成的 `..._已更名.docx` 副本，**手工删除两个原件**；或对原件执行一次 Word 内查找替换。

**若被问到为何改名（答辩话术，见执行包 §4.1）**：

> "我们最初用 EatSense，做文献调研时发现它与 2023 年爱丁堡大学的一个饮食行为识别数据集重名，另一个候选 ChewSense 也已被同类工作占用。为避免检索混淆，改成了 AcouDiet。**这次尽调也让我们系统梳理了相关工作，反而帮我们把定位想清楚了。**"

> ⚠️ **不要回避更名这件事**。建议在 PPT 直接放一页"命名与查重"（执行包 §5 有模板）——**主动交代传达的是严谨，不是犯错。**

#### 3.0.5 必补章节：相关工作（Related Work）

这是国家级奖项的硬门槛，也是把"重名危机"转成"调研充分"的机会。**建议按下列结构写**：

```
2.x 相关工作
  2.x.1 视觉饮食识别（拍照 → 食物分类）—— 成熟但需主动操作、暴露环境隐私
  2.x.2 可穿戴声学饮食识别 —— AutoDietary[2016]、ChewSense[2025]、NutriEar[2025]、AudiNosh[2025]
        ▸ 共同点：精度高，但依赖专用/耳戴硬件
        ▸ 局限：需额外购买设备、佩戴负担、日常依从性低
  2.x.3 公开声学数据集 —— Eating Sound Collection[Ma et al. 2020]（20 类 / 11,141 片段）
        ▸ 局限：安静环境、单人、固定麦克风距离，与手机真实场景存在域差异
  2.x.4 本文定位 —— 零额外硬件、手机端侧推理、面向长期依从性的健康管理闭环
```

**每一节都要落到"我们不同在哪"**，而不是罗列文献。

---

### 3.1 🔴 漏洞 2：Mel 特征时间窗数学自相矛盾（最致命的技术错误）

**证据（docx v1/v2 §5.1.1、§6.3.2、§6.3.3、`执行规划草稿.txt`）**：

| 参数 | 计划书写的值 |
|---|---|
| 采样率 | 16 kHz |
| 帧长 | 25 ms（400 samples） |
| 帧移 (hop) | 10 ms（160 samples） |
| n_fft | 1024 |
| n_mels | 128 |
| **音频窗长** | **3 s**（`执行规划草稿.txt`：`sample_rate=16000 duration=3s`） |
| **Mel 输出** | **128 × 128** |

**矛盾计算**：

- 3 s 音频按 hop=160 → `3.0 × 16000 / 160 = 300` 帧，Mel 图应为 **128 × 300**，不是 128×128。
- 反推 128 帧对应的时长：`128 × 160 / 16000 = 1.28 s`（用计划书的 hop=160）
  或 `128 × 512 / 16000 = 4.096 s`（用计划书的 n_fft=1024 → hop 通常 = n_fft/2 = 512）。

**即：计划书里 `3 s`、`hop=160`、`128×128` 三个数不可能同时成立。**

**后果**：模型训练与 App 端若各按一半理解去实现，**输入张量形状不一致，App 直接抛异常或输出恒定垃圾**。这是那种"Day 6 联调时才炸、然后花两天找不到原因"的 bug。

**处置：冻结唯一一组自洽参数（本方案强制）**

```json
{
  "sample_rate": 16000,
  "n_fft": 1024,
  "win_length": 1024,
  "hop_length": 512,
  "n_mels": 128,
  "fmin": 20,
  "fmax": 8000,
  "window": "hann",
  "power": 2.0,
  "db_range": 80.0,
  "top_db": 80.0,
  "patch_frames": 128,
  "patch_seconds": 4.096,
  "inference_hop_seconds": 0.5,
  "input_shape": [1, 128, 128, 1],
  "normalization": "per_patch_minmax_to_[0,1]"
}
```

**推导（可直接放进答辩 PPT，这是加分项）**：
- `128 帧 × 512 hop / 16000 Hz = 4.096 s`
- 4.096 s 窗口在 16 kHz 下覆盖约 **4–8 个咀嚼周期**（咀嚼频率 1–2 Hz），足以体现"瞬态冲击 + 频率分布 + 时间结构"三类判别信息
- 推理时每 **0.5 s** 滑动取一个 patch → 每秒产出 2 个预测 → 配合 §3.6 的投票机制
- **不要再用 `3 s` 这个数字**

> 附带收益：4.096 s 比 3 s 更贴近"一口食物"的真实时长，答辩时也更好解释。

### 3.2 🔴 漏洞 3：PyTorch → ONNX → TFLite 转换链在 MobileNetV3 上大概率失败

**证据**：docx §13 D4、`讨论2.txt` §11 D4、`执行规划草稿.txt` D4 均写"PyTorch → ONNX → TFLite"。

**技术事实**：MobileNetV3 含两类 TFLite 转换不友好的结构：
1. **Hardswish / Hard-sigmoid** 激活 —— ONNX `HardSwish` 算子在 TFLite 转换器中长期是已知痛点
2. **Squeeze-and-Excitation (SE) 模块** —— 含 `GlobalAveragePool` + reshape 链，ONNX→TF 图重写时容易产生不受支持的模式

**并且**：即便转换"成功"，也常见**静默精度损失**（转换不报错，但输出分布畸变）。

**处置（决策已冻结）**：

| 优先级 | 路线 | 说明 |
|---|---|---|
| **主路线** | **直接用 TensorFlow / Keras 训练** → 保存 SavedModel → `TFLiteConverter.from_saved_model()` | 零转换风险。Keras 有 `keras.applications.MobileNetV3Small`，或自定义等价的轻量 CNN |
| 备用路线 A | PyTorch 训练 → `onnx2tf` → TFLite | 转换质量优于原生 TF 转换器 |
| 备用路线 B | PyTorch 训练 → 自写等价 Keras 模型 → 权重迁移 | 适合必须用 PyTorch 训练的场景 |
| **不推荐** | PyTorch → ONNX → `TFLiteConverter.from_onnx()` | 失败率最高 |

**配套要求（比选哪条路线更重要）**：

> ⚠️ **必须建立"PyTorch 参考实现 vs TFLite 部署实现"的数值对齐测试**：
> 取 50 条固定音频，分别过两侧管线，逐条比较 Top-1 标签与置信度。
> **验收标准：Top-1 标签一致率 ≥ 98%，置信度最大绝对偏差 ≤ 0.05。**
> 不通过则不允许进入 App 联调阶段。
> 这是唯一能提前发现"静默精度损失"的手段。

### 3.3 🟠 漏洞 4：App 端在 Dart 里做 Mel 计算 —— 性能与精度双风险

**证据**：docx §9.3 写"AI Inference Module 负责 Mel 频谱计算（**可使用 dart:ffi 调用 C++ 实现加速**）"—— 说明作者自己也意识到 Dart 算 FFT 有问题，但没定论。

**风险**：
1. **精度**：Dart 侧 Mel 实现与 librosa 的边界处理、归一化、log 基数只要有一处不同，模型立刻失效。这是"精度崩塌型"bug。
2. **性能**：纯 Dart 做 128 点 Mel 滤波器组矩阵乘 + FFT，单帧开销可能达数十毫秒，叠加推理后无法满足实时。
3. **重复实现**：训练侧 Python + 部署侧 Dart 两套实现，是 bug 温床。

**处置（推荐方案，附带"免费"能力）**：

**在 Android 原生层（Kotlin）完成音频采集 + Mel 计算，只把 128×128 的 float 数组传给 Flutter。**

```
┌─────────────────────────────────────────────────────────┐
│ Android Native (Kotlin)                                  │
│  AudioRecord(16kHz, MONO, PCM_16BIT)                     │
│      ↓ 环形缓冲 4.096s                                   │
│  MelSpectrogram 计算（n_fft=1024, hop=512, n_mels=128）  │
│      ↓ float[128][128]                                   │
│  MethodChannel → Flutter                                 │
└─────────────────────────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────┐
│ Flutter (Dart)                                           │
│  tflite_flutter: Interpreter.run(float[1][128][128][1])  │
│      ↓ logits                                            │
│  投票聚合 → UI                                           │
└─────────────────────────────────────────────────────────┘
```

**为什么这是最优解**：
- **TensorFlow Lite Task Library for Android 自带 `AudioClassifier` + `TensorAudio`**，它已内置 Mel 前端，且其默认参数（16kHz / n_fft 1024 / hop 512 / n_mels 128）**与本方案冻结参数完全一致**。等于白送一套经过验证的原生实现。
- 延迟可控（原生 < 30 ms）
- Python 侧只需在训练时**严格对齐同一组参数**即可，无需写第二套算法

**强制约束**：无论选哪条路，**必须产出一份 `feature_config.json`**，训练脚本与 App 端**同时读取它**，禁止两边各写一份常量。

### 3.4 🟠 漏洞 5：UI 稿与模型能力严重不匹配（最大的"答辩送分/失分"点）

这是材料中**最容易被评委一句话击穿**的地方：UI 设计稿承诺了模型物理上给不出的信息。

| UI 元素 | 出处 | 问题 | 处置 |
|---|---|---|---|
| 雷达图：蛋白质 / 脂肪 / 膳食纤维 / 碳水化合物 | `软件UI界面设计图/1.png` | 声学**只能输出食物类别**，无法测出营养素构成 | **改为四维度行为雷达图**：饮食规律性 / 食物结构 / 零食控制 / 进食速度（正好对应 §3.8 的健康评分四维），四个轴的分数天然存在 |
| "已摄入能量 1286/2000 kcal" | `1.png` | 声学无法测重量→无法算热量。且 1286 kcal 这个精确数字需要份量输入 | 改标签为"**估算能量参考**"，值给**区间**（如 `约 1100–1400 kcal`），并标注"基于标准份量估计" |
| "食材多样性 8/12 种" | `1.png` | 8 类模型最多 8 种，分母 12 不可能达到 | 分母改为**当前模型类别数**；或直接换成"**本周记录次数**" |
| 识别记录含"番茄""土豆""鸡翅"等 | `2.png` 底部缩略图 | 超出 8 类模型能力 | 缩略图仅保留 8 类内的食物 |
| 记录条目："苹果 95 kcal / 全麦面包 120 kcal / 纯牛奶 100 kcal" | `4.png` | 同上。且"全麦面包"这种细分粒度声学不可能区分 | 改为"面包 ≈1 片（约 120 kcal，估算）"，**粒度与模型能力对齐** |
| "已坚持记录 7 天" | `5.png` | **Demo 时没有 7 天历史数据** | 准备**预置种子数据**（Demo 模式），或改为实时计数 |
| 检测页历史列表数值错位（苹果图标配 99%） | `3.png` | 视觉稿有明显的图标/数值对应错误 | 交付前必须清理 |
| 记录页条目文字串行（"08:果 / 1高蛋白高钙"） | `4.png`(旧版) vs `8.png`(新版) | 旧版存在文本截断/串位 | 以 `8.png`、`9.png`、`10.png` 新版为准；旧版 `4.png`/`5.png` **作废** |

**处置（架构定版已给出答案）**：`APP期望功能汇总/1.jpg` 的定版架构里，食物属性是**知识库查表**得出的。同理，**营养素与热量也必须来自知识库查表 + 标准份量假设**，并且必须在 UI 上**明确标注"估算"**。

> 🎯 **这反而是加分点**：答辩时主动说"我们不声称能测热量，营养数据来自食物知识库的标准份量估算，UI 上明确标注为参考值，不作为医学依据"——评委会认为团队**清楚自己的技术边界**，这正是 `讨论3.txt` 说的"成熟团队的表现"。

#### 3.4.1 UI 数据契约表（**给做 UI 的人的唯一真源**）

> **现实问题**：C 从 D3 起要做 3 个页面。如果没人告诉他"每个格子的数字从哪来"，他会照抄设计稿里的假数据，然后在 D8 接通真实数据时发现**一半的字段根本没有数据源**——这是最典型的返工场景。
>
> **解法**：D1 就交付这张表并冻结。**表里没写的字段，UI 上不许出现。**

| 页面 | 展示元素 | 数据源 | 字段 | 单位/格式 | 无数据时 |
|---|---|---|---|---|---|
| **首页** | 今日健康评分 | 评分引擎（§3.8） | `totalScore` | 整数 0–100 | `--` |
| 首页 | 评级（良好/一般/需改善） | 评分引擎 | `grade` | 枚举 | `--` |
| 首页 | 较昨日变化 | 评分引擎（昨 vs 今） | `delta` | `↑12 分` | 隐藏该行 |
| 首页 | 四维雷达图 | 评分引擎 | `[regularity, structure, snack, speed]` | 各 0–满分 | 空雷达 |
| 首页 | **估算能量参考** | 知识库 × 记录数 | `sum(kcal) ± 20%` | `约 1100–1400 kcal` | `暂无数据` |
| 首页 | ~~食材多样性 8/12 种~~ | — | **改为**：`本周记录次数` | `12 次` | `0 次` |
| 首页 | 今日记录列表 | SQLite | `DietRecord[]` | 时间 + 食物名 + 图标 | `今天还没有记录` |
| **检测页** | 实时波形动画 | 环形缓冲 RMS | — | 纯视觉 | 静默态 |
| 检测页 | 当前预测（未确认） | 推理引擎单 patch | `top1 + confidence` | `薯片 62%` 灰色 | `正在感知…` |
| 检测页 | 确认结果卡片 | 三级机制 Level 2 | `top1 + confidence + attribute` | `薯片 / 91% / 脆性食品` | 保持上一态 |
| 检测页 | 行为指标 | 行为分析模块 | `chews, avgInterval, duration, speed` | `约 45 次 / 0.7 秒 / 4 分 23 秒 / 偏快` | `--` |
| **记录页** | 时间轴条目 | SQLite | `time, food, kcalRange, confidence, attribute` | 见下方模板 | 空状态 |
| 记录页 | 顶部汇总条 | SQLite 聚合 | `今日总热量 / 已记录次数 / 零食次数` | `315 kcal / 3 次 / 1 次` | `0` |
| 记录页 | 本周小结卡片 | 评分引擎 | `weeklySummary` 文本 | 一句话 | `数据不足` |
| **报告页** | 7 天趋势折线 | SQLite 聚合 | `daily[kcal 或 score]` | 折线 | 空图 + 提示 |
| 报告页 | 四维评分下钻 | 评分引擎 | 四项分数 | `饮食规律 26/30` | `--` |
| 报告页 | 健康建议 | 规则引擎（§3.8） | `advice[]` | 文案列表 | `暂不生成建议` |

**记录条目的标准模板（消除 `4.png`/`8.png` 的粒度失控）**：

```
┌────────────────────────────────────────────┐
│ 12:20  面包                    ≈120 kcal   │
│        软性主食 · 1 片（估算）              │
│        置信度 88%                      ›   │
└────────────────────────────────────────────┘
```

**三条硬规则**：
1. **热量数字必须带份量描述与"估算"字样**，禁止孤立出现 `120 kcal`。
2. **食物粒度必须落在 6 类内**。禁止出现"全麦面包""纯牛奶""番茄""鸡翅"等超出模型能力的命名。
3. **无法从模型推导的营养素（蛋白质/脂肪/碳水/膳食纤维）一律不得出现在 UI 上**，除非明确标注"来自知识库估算"且不与模型置信度混淆。

#### 3.4.2 视觉稿修正清单（交付前逐张核对）

| 稿 | 位置 | 修正 |
|---|---|---|
| `1.png` | 雷达图 | 四轴改为：饮食规律性 / 食物结构 / 零食控制 / 进食速度 |
| `1.png` | "已摄入能量 1286/2000 kcal" | 改为"估算能量参考 **约 1100–1400 kcal**" |
| `1.png` | "食材多样性 8/12 种" | 改为"**本周记录 12 次**" |
| `1.png` | 评分 85 / `2.png` 评分 82 | **统一为同一套 Demo 数据**（消除 D-1 缺口） |
| `2.png` | 底部缩略图（番茄/土豆/鸡翅…） | 仅保留 6 类内食物 |
| `2.png` | "近 7 天"措辞 | 改为"**本周**"（与实现口径一致） |
| `3.png` / `10.png` | 检测页历史列表数值错位（苹果图标配 99%） | **已删除该模块**（§8.2.2 裁剪 #5），无需修正 |
| `4.png` / `5.png` | 文本截断、串位 | **作废**，以 `8.png`/`9.png`/`10.png` 为准 |
| `4.png` / `8.png` | 条目命名"全麦面包""纯牛奶""1高蛋白高钙" | 按 §3.4.1 标准模板重写 |
| `5.png` / `9.png` | "已坚持记录 7 天" | 改为动态计数（真实累积，见 §9.1） |
| `5.png` / `9.png` | "数据导出 CSV" | **已裁剪**（§8.2.2 #3），入口置灰标"v1.1" |
| `9.png` | "我的成就 ×2" | 降级为静态展示（§8.2.2 #4） |
| 全部 | "EatSense" Logo 与标题 | **待更名后统一替换**（§3.0.4 替换清单） |

### 3.5 🟠 漏洞 6：训练集与测试集未按"人"划分 → 数据泄漏

**证据**：docx §8.3 只写了 `训练 70% / 验证 15% / 测试 15%`，**未说明按什么维度划分**。`讨论的修改建议汇总.txt` 建议(4) 已明确指出这个问题："而且按人划分更好：Person A/B → training，Person C → test，这样如果测试集上的结果依然不错，可信度会高很多。"

**为什么致命**：
- 按**片段随机划分**时，同一次进食录音被切成的多个片段会**同时出现在训练集和测试集**。
- 模型会记住**这个人的咀嚼习惯、这个房间的混响、这支麦克风的频响**，而非食物本身的声学特性。
- 结果是**虚高的准确率**（`讨论1.txt` 预警的"测试集 99%、现场 60%"就是这个坑，差距可达数十个百分点）。评委问一句"你的测试集怎么划分的？"就穿了。

**处置（强制）**：

```
划分规则（三级，缺一不可）：
  1. 主体级划分：按「志愿者 ID」整体划分，同一人的数据只出现在一个子集
       train: Person A, B, D, E       (60%)
       val:   Person C                (15%)
       test:  Person F, G             (25%)
  2. 会话级隔离：同一次进食会话的相邻片段不跨子集
  3. 公共数据集：按「原始录音文件 ID」划分，不按切片划分
```

**报告要求**：测试集必须**完全不参与任何数据增强**（`discussion 建议(4)` 的原文要求），且测试结果需同时报告：

| 测试集 | 含义 | 三者关系（**不是预测值**） |
|---|---|---|
| 公共集 in-domain 测试集 | 与训练同域，说明模型学没学会 | 应显著**高于**跨域 |
| **自采手机测试集（跨域）** | **真实场景，这才是要宣传的数字** | 通常**明显低于** in-domain |
| 跨人测试集（Person F/G） | 泛化能力 | 通常介于两者之间 |

> 🔴 **重要更正（v3.1 自查发现的本方案自身错误）**：本表 v3.0 曾给出 `85–95%` / `55–80%` 这类**具体数值预测**。经复核，**这些数字只是我对同类任务的粗略经验，没有任何本项目实测支撑，不应作为计划依据** —— 而且这正是本方案 §7.4 批评过的"提前把 `>80%` 当既定成果"的同一种错误。**已删除具体数值，改为只描述三者之间的相对关系。**
>
> **正确做法**：
> 1. 计划书与 PPT 中**不出现任何预测准确率**；
> 2. 三个数字全部由 **D3 实测**产出；
> 3. 实测出来是多少就写多少，配 **Wilson 置信区间 + 每类样本数**；
> 4. 若实测不理想，按 §8.4 **CP1 降级开关**执行，**而不是改数字**。

> ⚠️ **绝对不要**把 in-domain 的高准确率当成果宣传。

### 3.6 🟠 漏洞 7：滑动窗口投票机制过于粗糙

**证据**：docx §5.1.1 步骤 6："取最近 10 帧的众数"；`执行规划草稿.txt` D6："连续两次 chips 才确认"。

**问题**：
1. **两处机制不一致**（10 帧众数 vs 连续 2 次），实现时必然扯皮。
2. **纯众数投票在类别不平衡时会偏向多数类**。
3. 无**置信度累积**概念，无法处理"边界帧"。
4. 无**平滑过渡**，UI 上的识别结果会闪烁跳动，观感很差。

**处置（升级为三级机制）**：

> **先统一"帧"这个术语（原方案的混乱根源）**：原方案把两种完全不同的时间单位都叫"帧"——
> ① **Mel 帧**（10–32 ms 级，属于特征提取内部概念）
> ② **patch**（4.096 s 级，即模型的一次输入）
> "取最近 10 帧的众数"这句话因此**无法判定是指 100 ms 还是 40 秒**。**本方案统一改用 "patch" 指代模型输入**，并明确推理节奏：

```
推理节奏（冻结，与 §4.1 一致）：
  patch 时长        = 4.096 s
  patch 滑动步长     = 0.5 s
  → 每秒产出 2 个预测
  → 一个 30 秒的进食过程产出约 60 个预测

Level 1 — 后验概率平滑
    对最近 N=5 个 patch 的 softmax 输出做指数移动平均（EMA, α=0.4）
    时间跨度 = 5 × 0.5s = 2.5 s
    作用：抑制单 patch 抖动

Level 2 — 稳定性判据（确认级）
    条件 A：EMA 后 Top-1 类别连续 M=4 个 patch 保持不变（= 2.0 s）
    条件 B：EMA 后 Top-1 概率 ≥ τ_confirm（初始 0.70，按实测调）
    同时满足才输出「确认结果」并写入日志
    作用：消除边界误判、避免 UI 闪烁

Level 3 — 低置信度人机协同（交互级）
    若 τ_low ≤ p_top1 < τ_confirm（初始 0.45 ≤ p < 0.70）：
        UI 显示「疑似 X，请确认？」→ 用户点「是/否」→ 写入日志 + 修正标记
    若 p_top1 < τ_low：显示「未识别到明确食物」，不写日志
    作用：产品逻辑更合理，答辩时可正面回答"误识别怎么办"

三档阈值（τ_low / τ_confirm）不是拍脑袋定的：
  → 在 D3 用自采跨域测试集的**置信度分布直方图**标定
  → 目标是让「错误且高置信」的比例尽可能低（宁可漏报，不可错报）
  → 标定过程与结果写进测试报告，这是答辩加分点
```

> **UI 响应性提醒**：按上述参数，**从开始进食到 UI 首次给出"确认结果"约需 4–5 秒**（1 个 patch = 4.096s + 2s 稳定判据，与窗口滑动并行）。这**不是延迟缺陷，而是窗口长度的必然结果**。Demo 脚本必须据此写（先展示波形动画与"正在感知…"，4–5 秒后出结果），**不要在 PPT 里承诺"2 秒内出结果"**（`执行规划草稿.txt` §10 有这句话，需删除）。
>
> 若现场需要更快的首次反馈：可用**未确认的实时预测**做动画（置信度条随 patch 更新），**确认结果**仍走 4–5 秒路径。两路并行展示，观感既快又稳。

> 这一条直接对应 `讨论1.txt` 的 Q3："系统不是替代专业营养管理，而是降低记录成本。对于低置信度结果，会提示用户确认。" —— **必须真的做出来，不能只在答辩时说**。

### 3.7 🟡 漏洞 8：谱减法降噪（Spectral Subtraction）可能损害而非提升性能

**证据**：docx §6.3.2："采用谱减法（Spectral Subtraction）进行基础降噪，估计噪声频谱并从信号频谱中扣除"。

**问题**：谱减法的前提是"噪声平稳、且能从不含信号的段落估计噪声谱"。但本项目里**咀嚼声本身就是宽带瞬态信号**，与餐具碰撞、环境噪声在频谱上高度重叠。激进谱减会：
- 削掉咀嚼声的高频脆裂成分（**恰恰是判别力最强的 2–8 kHz 特征**）
- 引入"音乐噪声"伪影，成为模型新的干扰源

**处置**：
1. **降噪设为可开关的实验变量，不作为既定架构**。
2. 消融实验（Ablation）三组对照：`无降噪` / `谱减法` / `仅高通+LUFS响度归一化`
3. **按实测结果决定**。若谱减法无提升（很可能），**从架构中删除**，只保留：
   - 预加重 `y[n] = x[n] − 0.97·x[n−1]`（保留，廉价且有效）
   - **响度归一化（LUFS 或 RMS）**（保留，这是应对不同手机麦克风增益差异的关键）
4. 答辩时呈现消融实验表格，比"我们用了谱减法"更有说服力。

### 3.8 🟡 漏洞 9：健康评分算法缺失，UI 却已展示分数

**证据**：
- UI `1.png` 直接显示"**85 +/100 分**"和"良好"
- docx 全文**没有任何评分公式**（只有散落的规则表 §5.4.2）
- `讨论的修改建议汇总.txt` 建议(2) 已预警："千万不要让评委问'82 是怎么算出来的？'然后答不上来"

**处置：冻结一份完全透明的 100 分制评分卡（本方案设计）**

| 维度 | 权重 | 满分 | 判据与打分函数 |
|---|---|---|---|
| **饮食规律性** | 30% | 30 | 以用户**自身历史**三餐时间为基线（非固定时刻），计算时间标准差 σ。`score = 30 × max(0, 1 − σ/90min)`。σ≤30min 满分，σ≥90min 零分 |
| **食物结构** | 30% | 30 | 记蔬果类（apple/carrot）与优质主食（bread）占比 p。`score = 30 × min(1, p/0.4)`。p≥40% 满分 |
| **零食控制** | 20% | 20 | 周零食次数 n。`score = 20 × max(0, 1 − n/10)`。n=0 满分，n≥10 零分 |
| **进食速度** | 20% | 20 | 平均咀嚼间隔 t（秒）。t≥0.8s → 20 分；t≤0.4s → 0 分；中间线性插值 |

**总分 = 四项之和（0–100）**

**双 UI 一致性要求**：UI `1.png` 的雷达图必须渲染**这四项**（`饮食规律 / 食物结构 / 零食控制 / 进食速度`），而不是营养素。这样：
- 点击"85 分"可以下钻看到 `饮食规律 26/30 · 食物结构 24/30 · 零食控制 15/20 · 进食速度 17/20`
- 雷达图与总分**来源一致**，评委无法击穿
- 这正是 `讨论的修改建议汇总.txt` 描述的"非常有产品感"

> **已知缺口 D-1**：材料中 `1.png` 与 `2.png` 的分数不一致（85 vs 报告页 82）。**Demo 数据必须统一**，建议统一为一套预置数据集。

### 3.9 🟡 漏洞 10：产品主张与真实使用方式自相矛盾

**证据**：docx §12.1 对比表写"记录方式：自动感知，**零操作**""使用成本：极低"；§11.2 写"将饮食记录的操作成本**降为零**"。

**问题**：实际实现中（docx §10.2 措施 6、§15 风险6）明确要求"**检测需用户主动点击开始，非后台持续监听**"。既然需要用户点"开始检测"和"停止检测"，就**不是零操作**。

评委问："你说零操作，那为什么还需要点开始？" —— 一句话就矛盾了。

**处置：把"零操作"精确化为"零记录负担"**

| ❌ 不要说 | ✅ 要说 |
|---|---|
| 记录成本降为零 | **把"逐条记录"的成本，降为"一次开关"的成本** |
| 完全无感 | **检测期间无感；检测由用户发起** |
| 零操作 | **零录入操作**（无需搜索、拍照、填份量） |

**并在答辩中主动交代这个权衡**（这是加分项）：

> "我们**刻意**没有做后台常驻监听。后台常驻虽然更'无感'，但会带来两个问题：一是持续占用麦克风，隐私与功耗上都不可接受；二是 Android/iOS 对后台麦克风有严格限制。所以我们选择'用户主动发起检测会话'，把成本从'每餐记录 7 步'降到'每次用餐点 1 次开关'。这是隐私、功耗与体验之间的明确取舍。"

**这一段话的价值**：把一个"漏洞"转化为"我们想过、权衡过、有理由"的设计决策。评委最喜欢的回答类型。

### 3.10 🟡 漏洞 11：10 天计划内部矛盾 + 4 项工作无归属

**证据 A —— 计划自相矛盾**：
- docx §13：`D5 Flutter 音频采集模块开发`
- `执行规划草稿.txt` D5："**不要做 UI。**只做：点击按钮 → 录 3 秒声音 → 调用模型 → 显示结果"
- `讨论4.txt` §9：`D5 Android录音+推理最小Demo`
三份材料对 D5 的定义完全不同。

**证据 B —— 无归属的关键工作**：

| 缺失工作 | 为什么必须做 | 原计划归属 |
|---|---|---|
| **自采数据采集** | 跨域测试集、Demo 备用素材、`讨论4.txt` 说 3 人 × 4 类 × 3 场景 | **无！** 但 D2 就开始训练了 |
| **三种 Demo 模式开发** | `讨论1.txt`/`讨论2.txt`/`执行规划草稿.txt` 都强调，现场失败概率 30–50% | **无！** 只在风险表里提了一句 |
| **特征数值对齐测试** | §3.2 的静默精度损失只有靠它发现 | **无** |
| **消融实验与测试报告** | `讨论的修改建议汇总.txt` 建议(5) 明确要求 | 隐含在 D3/D9，无明确工时 |
| **环境安装（D0）** | 本机 Flutter/Android SDK 全缺（§2.1） | **无**，被当成 0 成本 |

**证据 C —— 团体分工与工时不匹配**：`执行规划草稿.txt` §7 分 A(AI)/B(Android)/C(产品) 三人。但 docx §4.1 的 MVP 有 **9 个功能模块**，其中"5 个核心页面完整 UI"（登录/首页/检测/记录/报告）本身就是一个独立工作量。**3 人 10 天做 9 模块 + 5 页面 + 模型训练 + 文档，工时超支约 30–40%。**

**处置**：见 §8 的倒排计划表（已重排，并补齐上述 5 项无归属工作）。

### 3.11 🔵 优化 12：数据集与训练域/测试域的系统性差异未被量化

**证据**：`讨论1.txt` §3 已准确预警，但 docx 中**没有量化**，只有"数据增强"这一招。

**事实**：
- Eating Sound Collection（[Ma et al. 2020, ACM](https://dl.acm.org/doi/abs/10.1145/3395035.3425656)，[PDF](https://ada.liacs.nl/papers/MaEtAl20.pdf)）：20 类食物、11,141 片段，**安静环境、单一食物、近距离定向麦克风**
- 本项目目标域：手机距嘴 10–50 cm、有人说话、餐具碰撞、混响、AGC 压缩、蓝牙编解码

**处置（把风险变成研究亮点）**：

1. **明确声明域差异**，在 PPT 上画一张"源域 vs 目标域"对比图。
2. **三级实验矩阵**（这是拿奖的关键材料）：

| 实验 | 训练集 | 测试集 | 说明什么 |
|---|---|---|---|
| E1 | 公共集 | 公共集 | 上限参考 |
| E2 | 公共集 | **自采手机集** | **域差异有多大（核心数字）** |
| E3 | 公共集 + 自采集 | **自采手机集（跨人）** | 域适应的收益 |

3. **增强策略必须服务跨域**：优先"环境噪声混合（SNR 5–20 dB）+ 混响（RIR）+ 随机增益"，其次才是 SpecAugment / Mixup。
4. **增强用的噪声库要自采**：录 10–20 分钟食堂/办公室/街道环境音，比用公开噪声库更贴合目标域，且零成本。

### 3.12 🔵 优化 13：其他具体小问题

| 编号 | 问题 | 位置 | 处置 |
|---|---|---|---|
| N-2 | `讨论4.txt` §5 提出"一个模型两个输出头：食物分类 + 食物属性分类" | 与定版架构冲突 | **已由定版架构否决**。属性走知识库查表。多任务在小数据上会互相干扰 |
| N-3 | docx §6.3.3 写"连续 128 帧拼接为 128×128" | §6.3.3 | 与 §3.1 同一错误。按新参数改为 128 帧 = 4.096 s |
| N-4 | §7.1 写"模型文件约 10MB (FP32)" | 与"参数量约 2.5M"不一致 | MobileNetV3-Small 实际约 **1.5M 参数**，FP32 约 **6 MB**，INT8 约 **1.5–2.5 MB**。数字要改准，否则被问就露 |
| N-5 | `讨论1.txt` 说 CNN14 约 80M 参数 | 大致正确 | CNN14 实际约 **81M 参数 / ~320 MB**。这个对比数据可用，建议在 PPT 列出作为"为何选轻量模型"的论据 |
| N-6 | §5.3.1 进食时长"连续 30 秒无进食声则结束" | 逻辑冲突 | 与"咀嚼时长 4 分钟"场景冲突：连续吃 4 分钟中间必有 <30s 间隔。**改为"连续 90 秒静音结束"**，并说明选择依据 |
| N-7 | §8.3 未提类别不平衡 | 数据方案 | 11,141 片段在 20 类间分布不均；选 6–8 类时更要注意。**必须报告每类样本数 + 宏平均 F1 + 每类召回**，而非只报总体 accuracy |
| N-8 | §10.2 措施 2"原始音频立即从内存释放" | 隐私设计 | 部分成立。Flutter `record` 插件在录音时可能写临时文件。**必须在代码中显式删除 `cacheDir` 临时音频**，并在答辩时说清是"内存缓冲 + 临时文件双清理" |
| N-9 | §8.2 招募 5–10 名志愿者 | 伦理合规 | 涉及人体数据采集，**必须提供书面知情同意**（模板见 §7.4）。无同意书的作品在国家级赛事上有合规风险 |
| N-10 | 5 个页面 = 登录 + 首页 + 检测 + 记录 + 报告 | docx §4.1 | 登录页对竞赛 Demo 是纯负担（无后端、无账号体系）。**砍掉登录页**，改为"本地用户档案"，省半天工时（`讨论2.txt` 也说"不要超过 5 个页面"，实际 4 页更优） |
| N-11 | docx §12.1"原创性/竞争优势"表 | 声称过强 | 竞品对比需有依据。将"AI 推理位置：多为云端处理"改为"多数饮食记录 App 为手动输入或云端视觉识别"。**避免无法证实的绝对化表述** |
| N-12 | `软件UI界面设计图/4.png`、`5.png` 存在文本截断/串位 | 旧版视觉稿 | 以 `8.png`、`9.png`、`10.png` 新版为准，**旧版作废**并归档 |

---

## 4. 冻结的技术规格（Single Source of Truth）

> 以下规格一经冻结，**10 天内不再变更**。任何变更须走"变更记录"并同步 AI 组与 App 组。

### 4.1 音频与特征

| 项 | 值 |
|---|---|
| 采样率 | 16000 Hz |
| 声道 / 位深 | 单声道 / PCM 16-bit |
| 预加重 | 0.97 |
| 窗函数 | Hann |
| n_fft / win_length | 1024 |
| hop_length | 512 |
| n_mels | 128 |
| fmin / fmax | 20 Hz / 8000 Hz |
| 幅度 | power spectrogram (power=2.0) |
| 压缩 | `power_to_db(ref=np.max, top_db=80)` |
| patch 帧数 | 128 |
| **patch 时长** | **4.096 s** |
| 推理滑窗步长 | 0.5 s |
| 归一化 | patch 内 min-max 映射到 [0,1] |
| 模型输入张量 | `float32[1, 128, 128, 1]` |

#### 4.1.1 ⚠️ 参数变更传播表（**防止"改了这里没改那里"**）

> **为什么必须有这张表**：§3.1 的参数修正不是"改一个数字"，而是**一条会传播到全项目的变更**。原计划书里 `3s`、`hop=160`、`128×128` 三个数分散在 §5.1.1 / §6.3.2 / §6.3.3 三处，`执行规划草稿.txt` 里还有一处。**只改一处 = 埋雷。**

**变更单（执行时必须逐行打勾）**：

| # | 位置 | 旧值（错误） | 新值（冻结） | 负责人 | ✅ |
|---|---|---|---|---|---|
| P-1 | 计划书 §5.1.1 步骤 2 | hop 10ms / 帧移 160 | **hop 512（32ms）** | A | ☐ |
| P-2 | 计划书 §5.1.1 步骤 4 | "128×128 Mel 频谱图" | **128×128（128 帧 × 4.096s）** | A | ☐ |
| P-3 | 计划书 §6.3.2 | 帧长 25ms / 帧移 10ms | **win 1024（64ms）/ hop 512** | A | ☐ |
| P-4 | 计划书 §6.3.3 | "连续 128 帧拼接" | **128 帧 = 4.096 s** | A | ☐ |
| P-5 | `执行规划草稿.txt` D1 | `duration=3s` / `hop_length=512` | **`patch_seconds=4.096`**（hop 原本就对） | A | ☐ |
| P-6 | §5.4 行为分析 | 4.096s patch 时域 | 与 Mel 共用同一环形缓冲 | B | ☐ |
| P-7 | **§8.3 D5 描述** | "录 3 秒声音" | **"录 4.096s（或连续流）"** | B | ☐ |
| P-8 | **§14 P0 B 技能预演** | "Kotlin 录 3 秒 PCM" | **"Kotlin 录 4.096s PCM"** | B | ☐ |
| P-9 | `feature_config.json` | — | 单一真源，含全部上述参数 | A+B | ☐ |

> 🔴 **P-6/P-7/P-8 是本方案 v3.1 自查时发现的"自身遗漏"** —— v3.0 冻结了 4.096s，但 §8.3 与 §14 仍写着"3 秒"，正是 §3.1 描述的那类联调地狱。**已在本版一并修正。**

**验收方式（机械可查）**：全项目搜索 `3s`、`3 秒`、`hop.*160`、`帧移 10`，**命中数必须为 0**（历史对比表除外）。

#### 4.1.2 两条硬性实现约束

| 约束 | 说明 | 违反后果 |
|---|---|---|
| **Mel 参数两侧必须同源** | 训练脚本与 App 端**都读 `shared/feature_config.json`**，禁止各写一份常量 | 精度骤降到随机水平，且不报错 |
| **归一化必须逐 patch 计算** | 若用 `ref=np.max`，训练与推理都必须**逐 patch** 算 max；推荐改为**固定 dB 范围**（`clip(x, -80, 0)`）以规避域偏移 | 夜间/安静环境输入分布与训练不一致 → 准确率崩塌 |

### 4.2 待训练模型

| 项 | 值 |
|---|---|
| 架构 | MobileNetV3-Small（**TF/Keras 实现**，见 §3.2） |
| 输入 | `128 × 128 × 1`（单通道） |
| 输出 | 6 类（见 §4.3），Softmax |
| 参数 | ~1.5 M（以实测为准） |
| 体积目标 | FP32 ≤ 6 MB / **INT8 ≤ 2.5 MB** |
| 训练 | AdamW，lr 1e-3 → 1e-5，CosineAnnealing，batch 32，≤50 epoch，早停 patience=5 |
| 损失 | CrossEntropy + LabelSmoothing(ε=0.1) |
| 迁移 | ImageNet 预训练 → 冻结底层 → 解冻微调 |

### 4.3 识别类别（6 类冻结 · 2 类储备）

**冻结 6 类**（依据 `执行规划草稿.txt`，理由：声学特征差异明显 + 公共集样本充足 + 现场演示效果好）

| ID | 英文 | 中文 | 属性（知识库） | 难度 |
|---|---|---|---|---|
| 0 | chips | 薯片 | 脆性/高加工零食 | 易 |
| 1 | apple | 苹果 | 脆爽果蔬 | 易 |
| 2 | cookie | 饼干 | 脆性/高加工零食 | 易 |
| 3 | bread | 面包 | 软性主食 | 易 |
| 4 | carrot | 胡萝卜 | 脆爽蔬菜 | 易 |
| 5 | drink | 饮料 | 液体 | 易 |

**储备 2 类**（若 D3 模型准确率达标则加入，否则放弃）
| 6 | nuts | 坚果 | 硬脆食品 | 中 |
| 7 | noodles | 面条 | 软性主食 | 中 |

> **降级开关（触发条件可在 D3 现场标定，不必现在拍板）**：
> - **判据**：D3 实测的跨域准确率，以及"现场连续 10 次实拍检测的成功次数"
> - **若现场成功率 < 6/10** → 降为 4 类（`脆性 / 软性 / 果蔬 / 液体`），这类粗粒度区分对域偏移更鲁棒
> - **若降为 4 类后仍 < 6/10** → 模型退居次要，Demo 主张转向"**行为分析**"（`执行规划草稿.txt` §9 已给出这条路径）
> - **原则**：判据服务于"现场能不能演成功"，而不是追求某个准确率数字。**先用 D3 的数据看清现实，再定阈值。**

### 4.4 平台与交付

| 项 | 值 |
|---|---|
| 框架 | Flutter 3.24.x + Dart |
| **v1.0 交付平台** | **Android only**（`minSdk 24`, `targetSdk 34`） |
| iOS | 架构兼容，**不验证、不承诺** |
| 推理 | `tflite_flutter` + `XNNPACK`；NNAPI 作可选加速（见风险 R-1） |
| 特征计算 | Android 原生 Kotlin（§3.3） |
| 本地存储 | SQLite (`sqflite`) —— **不用 Hive**（理由见下） |
| 状态管理 | Riverpod |
| 图表 | `fl_chart`（雷达图 + 折线图） |

> **决策修正 N-13**：docx §9.3 写 Hive，§9.4 写 `Hive / SQLite`。**建议统一为 SQLite (`sqflite`)**。理由：本项目需要**按时间范围聚合统计**（周报、趋势、规律性分析），SQL 的 `GROUP BY` / 时间窗口查询远优于 NoSQL 遍历；且 CSV 导出用 SQL 更直接。

---

## 5. 工程实现方案

### 5.1 仓库结构

```
EatSense/
├── README.md
├── docs/
│   ├── 01_项目计划书.md
│   ├── 02_技术方案.md
│   ├── 03_测试报告.md
│   ├── 04_答辩QA.md
│   └── 05_伦理与知情同意.md
├── shared/
│   └── feature_config.json          ★ AI 组与 App 组共同读取，唯一真源
├── ai/                               # Python
│   ├── requirements.txt
│   ├── data/
│   │   ├── raw/                      # 公共集 + 自采集
│   │   ├── noise/                    # 自采环境噪声（增强用）
│   │   ├── rir/                      # 房间冲激响应
│   │   └── splits/
│   │       ├── train.csv             # subject_id 级划分，禁止泄漏
│   │       ├── val.csv
│   │       └── test_public.csv / test_mobile.csv
│   ├── src/
│   │   ├── config.py                 # 读取 feature_config.json
│   │   ├── features.py               # Mel 提取（唯一实现）
│   │   ├── dataset.py                # Dataset + 增强
│   │   ├── augment.py                # 噪声/增益/RIR/SpecAugment/Mixup
│   │   ├── model.py                  # MobileNetV3-Small (Keras)
│   │   ├── train.py
│   │   ├── evaluate.py               # 混淆矩阵 + P/R/F1 + Wilson CI
│   │   ├── ablations.py              # 降噪/增强 消融实验
│   │   └── export_tflite.py          # INT8 量化导出
│   ├── scripts/
│   │   ├── collect_check.py          # 自采数据规范性检查
│   │   └── parity_test.py            ★ PT/Keras vs TFLite 数值对齐测试
│   └── artifacts/
│       ├── model_fp32.tflite
│       ├── model_int8.tflite
│       ├── confusion_matrix.png
│       └── metrics.json
└── app/                              # Flutter
    ├── pubspec.yaml
    ├── assets/
    │   ├── models/eatsense_int8.tflite
    │   ├── demo_audio/               ★ 现场 Demo 备用音频（自采）
    │   └── foods.json                # 食物知识库
    ├── android/app/src/main/
    │   ├── AndroidManifest.xml       # 仅 RECORD_AUDIO，无网络权限
    │   └── kotlin/.../audio/
    │       ├── AudioCapture.kt        # AudioRecord
    │       ├── MelFrontend.kt         ★ Mel 计算（对齐 feature_config）
    │       └── AudioPlugin.kt         # MethodChannel
    └── lib/
        ├── main.dart
        ├── core/
        │   ├── config/feature_config.dart
        │   └── router.dart
        ├── data/
        │   ├── db/ (sqflite, 迁移脚本)
        │   ├── models/ (DietRecord, BehaviorMetrics)
        │   └── repo/ (DietRepo, StatsRepo)
        ├── domain/
        │   ├── inference/             # tflite 封装 + 投票聚合 (§3.6)
        │   ├── behavior/              # 咀嚼计数/节律/时长/速度 (§5.5)
        │   ├── knowledge/             # 食物知识库查表
        │   └── health/                # 评分卡 + 规则引擎 (§3.8)
        ├── features/
        │   ├── home/                  # 首页
        │   ├── detect/                # AI 检测
        │   ├── log/                   # 饮食记录
        │   ├── report/                # 健康报告
        │   └── profile/               # 我的
        └── demo/                      # ★ Demo 模式（三档降级）
```

### 5.2 AI 侧关键实现

**`features.py` —— 唯一实现，训练与验证共用**

```python
import json, numpy as np, librosa

CFG = json.load(open("shared/feature_config.json"))

def wav_to_patch(y: np.ndarray) -> np.ndarray:
    """4.096s waveform @16k -> (128,128) float32 in [0,1]"""
    assert len(y) == int(CFG["sample_rate"] * CFG["patch_seconds"]), \
        f"expect {int(CFG['sample_rate']*CFG['patch_seconds'])} samples"
    S = librosa.feature.melspectrogram(
        y=y, sr=CFG["sample_rate"], n_fft=CFG["n_fft"],
        hop_length=CFG["hop_length"], n_mels=CFG["n_mels"],
        fmin=CFG["fmin"], fmax=CFG["fmax"], power=CFG["power"],
    )
    S_db = librosa.power_to_db(S, ref=np.max, top_db=CFG["top_db"])
    assert S_db.shape == (CFG["n_mels"], CFG["patch_frames"]), S_db.shape
    lo, hi = S_db.min(), S_db.max()
    return ((S_db - lo) / (hi - lo + 1e-8)).astype(np.float32)
```

> ⚠️ **注意 `ref=np.max` 的域偏移陷阱**：`ref=np.max` 是**每个 patch 各自归一化**，因此推理时也必须**逐 patch** 算 `np.max`，不能复用训练集全局最大值。这一点必须在 `parity_test.py` 中覆盖。若嫌麻烦，改用**固定 dB 范围截断**（如 `S_db = np.clip(S_db, -80, 0)`，ref 用固定常数），跨域稳定性更好 —— **本方案推荐固定范围方案**。

**`parity_test.py` —— 部署前最后一道闸门**

```python
"""对齐测试：训练侧管线 vs TFLite 部署管线，逐条比对 Top-1 与置信度"""
THRESH = {"label_match": 0.98, "max_conf_delta": 0.05}

def run(n_samples=50):
    mismatches, deltas = [], []
    for wav in sample_wavs(n_samples):
        y = load_16k(wav)
        ref = model_keras(wav_to_patch(y)[None, ..., None])      # 训练侧
        got = tflite_interpreter(wav_to_patch(y)[None, ..., None])  # 部署侧
        if ref.argmax() != got.argmax():
            mismatches.append(wav)
        deltas.append(abs(ref.max() - got.max()))
    assert 1 - len(mismatches)/n_samples >= THRESH["label_match"], mismatches
    assert max(deltas) <= THRESH["max_conf_delta"], max(deltas)
    print(f"✅ 对齐通过：标签一致率 {1-len(mismatches)/n_samples:.1%}, "
          f"最大置信度偏差 {max(deltas):.4f}")
```

### 5.3 Android 原生 Mel 前端（关键路径）

**推荐做法**：优先直接用 **TFLite Task Library 的 `AudioClassifier` + `TensorAudio`**，其内置 Mel 前端默认参数就是 `16kHz / n_fft 1024 / hop 512 / n_mels 128`，与本方案冻结参数一致。

```kotlin
// 若用 Task Library：几乎零成本
val opts = AudioClassifier.AudioClassifierOptions.builder()
    .setScoreThreshold(0.0f)                 // 阈值交给 Dart 侧投票逻辑
    .setMaxResults(6)
    .build()

// 若需自实现（Task Library 版本不合或要自定义参数）：
class MelFrontend(private val cfg: FeatureConfig) {
    // 1. 预加重  y[n] = x[n] - 0.97*x[n-1]
    // 2. 分帧    128 帧，hop=512，Hann 窗，n_fft=1024
    // 3. FFT     用 JTransforms 或手写 radix-2
    // 4. Mel     128 个三角滤波器 (20Hz-8kHz)，矩阵预计算缓存
    // 5. log     10*log10(max(x, 1e-10))，固定 dB 截断 [-80, 0]
    // 6. 输出    FloatArray(128*128) 行主序
}
```

> ⚠️ **风险 R-FFT**：自实现 FFT 的**窗函数边界与归一化系数**必须与 librosa 逐位对齐。**验收方式**：同一条 wav，Python 侧输出 `mel.npy`，Kotlin 侧输出 `mel.bin`，在 Python 里 `np.allclose(a, b, atol=1e-3)`。这个测试写进 D5 的验收标准。

### 5.4 行为分析模块（咀嚼计数等）

```
输入：4.096s patch 的时域波形（来自同一环形缓冲）

步骤：
  1. 计算短时 RMS 能量包络（帧长 10ms, hop 5ms）
  2. 滑动平均平滑（窗长 50ms）→ 去除毛刺
  3. 峰值检测：scipy.signal.find_peaks
       - 最小峰间距 = 200ms（对应最高 5 Hz 咀嚼率，人类上限约 2 Hz）
       - 最小峰高 = 动态阈值 = μ + 0.5σ（基于本会话能量分布）
  4. 伪峰过滤：
       - 排除峰宽 > 150ms 的宽峰（吞咽声特征）
       - 排除孤立峰（前后 300ms 内无其他峰 → 环境噪声/餐具碰撞）
  5. 计数输出
```

**指标定义（必须与 UI 文案一致）**：

| 指标 | 定义 | 展示措辞 |
|---|---|---|
| 咀嚼次数 | 累计有效峰个数 | "约 45 次"（**必须带"约"**） |
| 平均咀嚼间隔 | 相邻峰间隔均值 | "平均 0.7 秒" |
| 咀嚼节律 | 间隔的标准差 σ | σ<0.15s → "节律稳定"；否则"节律波动" |
| 进食时长 | 首次进食声 → 连续 **90s** 无进食声（修正 N-6） | "持续 4 分 23 秒" |
| 进食速度 | 见下表 | "偏快 / 正常 / 偏慢" |

**进食速度判定（透明规则）**：
```
平均咀嚼间隔 t:
  t < 0.50s  → 偏快
  0.50–0.80s → 正常
  t > 0.80s  → 偏慢
（UI 展示同时给出判据，便于答辩下钻）
```

**验证要求**：人工标注 ≥10 段进食录音的咀嚼次数作为 ground truth，报告 **MAE（平均绝对误差）**。若 MAE > 25%，文案从"约 45 次"降级为"咀嚼节奏：较快"（**不给绝对数字**）。

### 5.5 食物知识库（`assets/foods.json`）

```json
{
  "chips": {
    "zh": "薯片", "icon": "chips.png",
    "attribute": "脆性食品", "category": "高加工零食",
    "standard_portion": { "desc": "1 小包（约 30g）", "kcal": 160 },
    "nutrition_tag": ["高油", "高盐"],
    "risk_note": "高加工零食，建议控制频率"
  },
  "apple": {
    "zh": "苹果", "icon": "apple.png",
    "attribute": "脆爽果蔬", "category": "水果",
    "standard_portion": { "desc": "1 个（约 180g）", "kcal": 95 },
    "nutrition_tag": ["膳食纤维", "维生素C"],
    "risk_note": "推荐食物"
  }
  // ... cookie / bread / carrot / drink
}
```

> **关键设计**：`kcal` 字段**永远与"标准份量"绑定展示**，UI 文案固定为 `约 1 小包 · 估算 160 kcal`。**不允许出现不带份量说明的孤立热量数字**。这是 §3.4 漏洞的根治方案。

---

## 6. 隐私与合规设计（升级版）

### 6.1 技术措施（可验证，不是口号）

| # | 措施 | 可验证方式 |
|---|---|---|
| 1 | 音频仅存在于内存环形缓冲 | 代码审查：无 `File.write` 到音频路径 |
| 2 | **录音临时文件强制清理** | `getCacheDir()` 启动时 + 会话结束时清空 `audio_*` 临时文件 |
| 3 | 数据库仅存结构化字段 | Schema 审查：无 BLOB 音频列 |
| 4 | **APK 不申请 INTERNET 权限** | `AndroidManifest.xml` + `aapt dump badging` 验证 |
| 5 | 仅申请 `RECORD_AUDIO` | Manifest 审查（无相机/位置/通讯录） |
| 6 | 检测需用户主动发起 | UI 流程审查，无后台 Service 常驻 |
| 7 | 一键清除全部数据 | 设置页「清空所有数据」按钮 + 实现 |

> 🎯 **措施 4 是最有说服力的一条**：在答辩现场用 `aapt dump badging app.apk | grep uses-permission` 展示"**这个 App 连联网权限都没有，物理上不可能上传你的声音**"。这比任何解释都有力。**强烈建议做进 PPT。**

### 6.2 数据采集伦理（N-9 处置）

面向 5–10 名志愿者的**知情同意书**必须包含：

1. 数据用途：仅用于本项目声学模型训练，不用于任何其他目的
2. 采集内容：进食过程音频（**不采集人声对话内容**）
3. 存储位置：仅存于项目组本地设备
4. 匿名化：以 `P01/P02…` 编号标识，不记录姓名/联系方式
5. **撤回权**：志愿者可随时要求删除其全部数据
6. **不留存人声**：录音中若含对话，标注阶段予以剔除
7. 数据不对外发布（若需发布，另行取得授权）

> 提交竞赛材料时，附上**已签署的同意书扫描件（姓名打码）**，是合规加分项。

---

## 7. 数据方案（修正版）

### 7.1 数据来源与规模

| 来源 | 用途 | 规模目标 | 归属 |
|---|---|---|---|
| Eating Sound Collection | 基础训练 | 6 类子集，约 3,000–4,000 片段 | AI 组 D0 |
| **自采手机数据** | 跨域测试 + Demo 素材 | **6 类 × 5 人 × 8 段 = 240 段**（见修正） | **产品组 D0–D1** |
| 自采环境噪声 | 增强 | 10–20 分钟（食堂/办公室/街道） | 产品组 D0 |
| 合成（增强）数据 | 鲁棒性 | 训练集在线生成，不落盘 | AI 组 D2 |

> **修正 N-14（针对 `讨论4.txt` 的"约 1000 条"）**：`讨论的修改建议汇总.txt` 建议(4) 已预警"不要为了凑数字"。**1000 段 / 3 人 = 每人 333 段，不现实**，且评委一问"每段多长？怎么切的？"就会露馅。
> **改为务实目标：6 类 × 5 人 × 每类 8 段 = 240 段**，每段 5–10 秒，覆盖 3 种采集姿态（桌面 30cm / 手持 / 近距离 10cm）。**这个数字真实、可解释、可复现**，且足够做跨域测试集（留出 2 人作为测试人）。

### 7.2 划分方案（禁止数据泄漏）

```
公共集（按原始录音文件 ID 划分，不按切片）：
    train 70% / val 15% / test_domain 15%

自采集（按人划分 ★ 关键）：
    P01, P02, P03  →  仅用于【跨域测试】test_mobile.csv   （绝不进训练）
    P04, P05       →  可用于【域适应微调】的验证集

禁止行为（写进代码注释 + 评审清单）：
  ✗ 同一次进食会话的片段跨子集
  ✗ 同一人的片段跨子集
  ✗ 测试集参与任何增强
  ✗ 用测试集调超参
```

### 7.3 增强策略（可按消融实验结果裁剪）

| 增强 | 参数 | 模拟 | 优先级 |
|---|---|---|---|
| 环境噪声混合 | SNR 5–20 dB，**自采噪声库** | 真实就餐环境 | **P0** |
| 随机增益 | 0.5–2.0× | 手机距离/麦克风差异 | **P0** |
| LUFS 响度归一化 | 目标 −23 LUFS | 设备增益差异 | **P0** |
| 混响 | 随机 RIR 卷积 | 房间声学 | P1 |
| 时间变换 | 拉伸 0.8–1.2× / 随机裁剪 | 不同进食速度 | P1 |
| SpecAugment | 时间掩码 T=10 / 频率掩码 F=8 | 频谱鲁棒性 | P1 |
| Mixup | α=0.2 | 泛化 | P2（小数据上可能有害，需消融） |

### 7.4 评估报告要求（修正 N-7）

**必须报告（缺一不可）**：

1. **总体 Top-1 准确率 + Wilson 95% 置信区间**
   > 自采测试集只有 240 段（每类 40 段）。**举例说明为何必须报区间**：若实测 60%（36/60），Wilson 95% CI 约为 **[47%, 72%]**——跨度 25 个百分点。**不报区间直接写"准确率 60.0%"，评委会算一下样本量就发现这个精度不可信。**
   ```python
   from statsmodels.stats.proportion import proportion_confint
   lo, hi = proportion_confint(k, n, method='wilson')
   ```
2. **每类 Precision / Recall / F1 + 宏平均 / 加权平均**
3. **混淆矩阵**（7 类含"未识别"或 6×6 纯分类）
4. **每类测试样本数**（暴露不平衡）
5. **in-domain vs 跨域 对照**（§3.11 的 E1/E2/E3）
6. **推理延迟**：单 patch 推理 ms（中端机实测），端到端确认延迟 s
7. **消融实验表**：降噪 on/off、各增强 on/off

---

## 8. 执行计划（倒排 + 硬性降级开关）

### 8.1 D0：环境与数据准备日（**新增，原计划缺失**）

| 组 | 任务 | 验收 |
|---|---|---|
| AI | 装 conda 环境 + CUDA + 依赖 | `check_env.py` 全绿 |
| AI | 下载 Eating Sound Collection，筛选 6 类 | `data/raw/` 有数据，类别分布表打印 |
| App | 装 Flutter + JDK17 + Android SDK | `flutter doctor -v` Android 全绿 |
| **产品** | **自采数据采集启动**（找志愿者 + 签同意书） | ≥2 人完成 ≥6 类录音 |
| **产品** | **自采环境噪声**（食堂/办公室/街道各 5 分钟） | `data/noise/` 有 3 个文件 |
| 全员 | 确认 `feature_config.json` 冻结 | 双组签字确认 |

> **D0 是并行日**，不占用 D1–D10 主线。

### 8.2 ⚠️ 范围冻结（3 人版必需 · 执行前必须签字确认）

**结论先说**：3 人 + 10 天，**不可能交付 docx §4.1 列出的全部 9 个功能模块 + 5 个完整页面**。原清单是"理想态"，实际工时超载约 35%。

若不做裁剪，必然出现的后果是：**D9 时 Demo 没跑通、报告页半成品、PPT 通宵赶**——这是学生竞赛最常见的死法。**裁剪不是妥协，是把工时集中到评委真正会看的地方。**

#### 8.2.1 三档判定表

| 档位 | 内容 | 3 人版处置 |
|---|---|---|
| 🔴 **不可砍**（砍了故事就不成立） | ①实时检测闭环 ②自动生成记录 ③健康报告+评分卡 ④parity_test ⑤三种 Demo 模式 | **全部保留** |
| 🟡 **可砍**（产品完整度损失，故事不受影响） | 手动修正入口、CSV 导出、个人页成就系统、检测页历史列表 | **砍掉** |
| 🔵 **可推迟**（写进 PPT"后续工作"） | 登录/账号、iOS、蓝牙耳机、多模态、LLM 助手、15–20 类扩展 | **推迟** |

#### 8.2.2 具体裁剪清单（共 7 项，释放约 3.5 人日）

| # | 原功能 | 出处 | 裁剪决定 | 释放工时 | 理由 |
|---|---|---|---|---|---|
| 1 | **登录页 / 账号体系** | docx §4.1（5 页面之一） | **删除**，改本地用户档案 | 0.5 人日 | 无后端、无账号体系，纯负担。N-10 |
| 2 | **手动修正**（改类别/删误报） | docx §5.2.2 | **降级**：仅保留 A/B 二选一确认（§3.6 Level 3），**不做任意改类别** | 0.5 人日 | 二选一确认已足够支撑"误识别怎么办"的答辩；完整修正需额外 UI + 数据模型 |
| 3 | **CSV 导出** | docx §5.2.2、UI `5.png` 数据导出 | **删除**，页面入口保留但置灰并标注"v1.1" | 0.3 人日 | 评委不会看 CSV；产品页有入口即可 |
| 4 | **个人页「我的成就」系统** | UI `5.png`、`9.png` | **降级为静态展示**（显示"已坚持 N 天"），不做成就解锁逻辑 | 0.5 人日 | 成就系统是纯 UI 逻辑，零技术含量、占工时 |
| 5 | **检测页「识别历史」列表** | UI `3.png`、`10.png` | **删除**（首页已有今日记录） | 0.3 人日 | 与记录页功能重复 |
| 6 | **RIR 混响增强 + Mixup** | docx §7.3、`讨论4.txt` §7 | **删除**（保留环境噪声+增益+LUFS+SpecAugment） | 0.3 人日 | 需额外找 RIR 数据集；小数据上 Mixup 可能有害（§7.3 已标 P2） |
| 7 | **咀嚼节律 σ（稳定性指标）** | docx §5.3.3 | **删除**，仅保留咀嚼次数 + 平均间隔 + 进食速度 | 0.3 人日 | 三项已足够支撑行为分析叙事；σ 的验证成本高（需人工标注 rhythm） |

**释放出的 3.5 人日，全部投入**：D5 端到端联调缓冲（+1 人日）、D9 三模式 Demo 实测（+1 人日）、D8 报告页与评分卡（+1 人日）、机动缓冲（+0.5 人日）。

#### 8.2.3 裁剪后的交付形态

```
页面数：5 → 4（首页 / AI检测 / 饮食记录 / 健康报告）
       「我的」并入首页右上角入口，不做独立 Tab
模型  ：6 类（降级开关：4 类）
增强  ：环境噪声 + 随机增益 + LUFS + SpecAugment（4 项，非 6 项）
行为  ：咀嚼次数 + 平均间隔 + 进食时长 + 进食速度（4 项，非 5 项）
报告  ：健康评分卡四维下钻 + 7 天趋势图（保留，这是产品感核心）
```

> **关键判断**：裁剪集中在**"评委看不到的完整度"**上，**不碰任何"评委要看的东西"**。实时检测、自动记录、健康报告、评分卡下钻、三模式 Demo、实验矩阵——**一个都不砍**。

#### 8.2.4 若 CP1 或 CP2 失败，追加降级阶梯

| 触发 | 追加裁剪 | 保留底线 |
|---|---|---|
| **CP1 未过**（跨域 <55%） | 6 类 → 4 类；报告页趋势图降为静态图 | 检测闭环 + 记录 + 评分卡 |
| **CP2 未过**（D5 闭环未通） | 砍报告页趋势动画、砍知识库细化、砍 UI 美化 | 检测 + 记录 + 三种 Demo 模式 |
| **CP3 未过**（D9 Demo 未全通） | **停止一切新功能**，全部 3 人扑 Demo 稳定性 | Mode B + Mode C 必须可用 |

### 8.3 D1–D10 主计划

| Day | AI 组（A） | App 组（B） | 产品组（C） | 当日硬验收 |
|---|---|---|---|---|
| **D1** | 数据清洗、切 patch、生成 splits（**按人/按文件**） | Flutter 工程初始化、路由、**4 页面骨架**、`feature_config.dart` 生成 | 继续自采；整理知识库 `foods.json`（6 类） | `dataset.py` 能吐出 `[B,128,128,1]` batch；`flutter run` 出空壳 App |
| **D2** | Baseline 训练（无增强），出 in-domain 指标 | **原生 AudioRecord + Mel 前端**（关键路径，全天） | 录制 Demo 备用音频（每类 5 段）；协助 C→UI 骨架搭建 | Baseline 可用；Kotlin 能输出 128×128 数组并打印 |
| **D3** | 增强训练 + 消融实验（**简化版：仅降噪 on/off**）；**跨域测试（E2）** | **Mel 数值对齐测试**（Python vs Kotlin） | 测试报告骨架；**接手首页 + 记录页 UI（占位数据）** | ★**跨域准确率出数**；Mel 对齐 `atol<1e-3` |
| **D4** | 训练收尾、**TFLite INT8 导出 + parity_test** | tflite_flutter 集成，加载模型跑通单条推理 | 答辩 QA 初稿；继续 UI 页面 | ★模型 ≤2.5MB；★**parity_test 通过** |
| **D5** | 支持 App 侧调参、延迟实测 | **端到端最小闭环**：录音→Mel→推理→显示（UI 可丑） | 整理自采数据标注；**检测页 UI** | ★真机点按钮出结果（**录 4.096s 连续流**，非"3 秒"） |
| **D6** | 协助定位跨域问题；训练报告 | 实时流式检测 + **三级投票机制（§3.6）** | Demo 脚本初稿；**报告页 UI（接评分卡占位）** | 连续吃 30s，结果稳定不闪烁 |
| **D7** | 出最终指标表/混淆矩阵/Wilson CI | **行为分析模块**（咀嚼次数/间隔/时长/速度） | 健康评分卡 + 规则引擎（**C 主写，B 只做接口**） | 能输出"约 45 次，偏快" |
| **D8** | 冻结模型、产出交付物 | 记录页 + 报告页 + 知识库 + 评分卡**接通真实数据** | 周报数据构造（用真实检测累积） | ★完整闭环：吃→识别→记录→报告 |
| **D9** | 测试报告定稿 | **三种 Demo 模式 + UI 美化 + 现场实测** | PPT + 演示视频 + 全流程演练 | ★**三种 Demo 模式全部实测通过** |
| **D10** | 待命支持 | **冻结代码，不改代码** | 现场答辩 | 提交材料齐全 |

> **D5 定义冲突（C-3）裁决**：采纳 `执行规划草稿.txt` 的定义 —— **D5 = 端到端最小闭环（UI 极简）**。理由：AI 与 App 的**联调风险是整个项目最大风险**，必须尽早打通。UI 美化推到 D9。

> **3 人版新增的关键安排（C 分担 UI）**：`C` 从 D3 起接手**首页 + 记录页 + 报告页的 UI 实现**（占位数据先行），B 专注原生音频/推理/集成。这是 3 人版能否成立的核心调度——Flutter 页面是声明式 UI，学习曲线远低于原生音频，C 完全可胜任。**若 C 不上 UI，B 必然在 D5–D8 崩盘。**

### 8.4 四个硬性检查点（Checkpoint）

| CP | 时间 | 判据 | 不达标时的动作 |
|---|---|---|---|
| **CP1** | **D3 晚** | 跨域实测结果 + **现场 10 次实拍成功率** | 成功率 <6/10 → **降为 4 类**；仍 <6/10 → 主张转向"行为分析"（§4.3 降级开关） |
| **CP2** | **D5 晚** | 端到端闭环是否跑通 | 未通 → D6 全天扑联调，UI 与报告页砍到最简（§8.2.4） |
| **CP3** | **D9 午** | 三种 Demo 模式是否全部可用 | 未通 → **停止一切新功能**，3 人全部扑 Demo 稳定性 |
| **CP4（3 人版新增）** | **D7 晚** | 报告页数据是否已接通真实记录 | 未通 → 启用预置演示数据集兜底（§9.1），放弃"真实累积"方案 |

> **CP1 为何不写死准确率阈值**：写死阈值（如"<55% 就降级"）看似严谨，实则**用假精度掩盖真问题** —— 我们并不掌握该任务的真实难度分布。改为**以"现场能否演成功"为判据**（10 次实拍成功次数），这个指标：①直接对应项目成败 ②可当场测量 ③不受样本量影响。**准确率仍然要如实报告，但它不是决策开关。**

### 8.5 团队分工（3 人 · 已冻结）

| 角色 | 负责范围 | 技能要求 | 负载峰值 |
|---|---|---|---|
| **A · AI 算法与数据** | 数据清洗/划分、特征提取、模型训练、消融实验、跨域评估、TFLite INT8 导出、parity_test、全部指标与图表 | Python / PyTorch 或 TF / librosa / 音频基础 | **D1–D4 满负荷**，D5–D9 转为支撑 |
| **B · 端侧与集成（唯一瓶颈）** | **Kotlin AudioRecord + Mel 前端 + MethodChannel**、tflite_flutter 集成、三级投票机制、行为分析模块、数据库、三种 Demo 模式、延迟优化 | Kotlin / Android / Flutter 集成 / TFLite | **D2、D5、D9 三个峰值，全程不可替代** |
| **C · 产品、UI 与文档** | 自采数据采集与标注、同意书、食物知识库、健康评分卡与规则引擎、**首页/记录页/报告页 UI 实现**、PPT、演示视频、Demo 脚本、答辩排练 | Flutter 基础 UI（D0–D1 速成）/ 产品设计 / 文案 | **D0–D1 满负荷**，D3–D8 转 UI 主力 |

#### 8.5.1 关键路径归因（3 人版唯一风险点）

```
B 的任务链（不可并行、不可替代）：
  D2 Kotlin Mel 前端 ──→ D3 Mel 对齐测试 ──→ D4 tflite 集成 ──→ D5 端到端闭环 ★CP2
                                                                    ↓
                                              D6 投票机制 ──→ D9 三种 Demo ★CP3
```

**B 是整个项目的单点故障。** 必须有两条保险：

1. **D2 前完成技能预演**：B 在 D0 必须先跑通一个"Kotlin 录 **4.096s** PCM 并打印 RMS"的最小 demo。若 D0 结束仍跑不通，**立即启用 Plan-S**（见下）。
2. **Plan-S（技能兜底方案）**：若 B 在 D2 结束仍未产出 128×128 数组，**立刻切换备用路线** —— 放弃自写 Kotlin Mel，改用 **TFLite Task Library 的 `AudioClassifier`**（其内置 Mel 前端参数与本方案冻结值一致，见 §5.3）。这条路线把 B 的工作量从"写 FFT + Mel 滤波器组 + 对齐调试"压缩为"调用现成 API"。**Plan-S 必须在 D0 就调研清楚可行性，不能等到 D2 才临时找。**

#### 8.5.2 3 人版的协作规则（避免互相阻塞）

| 规则 | 说明 |
|---|---|
| **接口先行** | D1 结束前，A/B/C 三方冻结三个接口：①`feature_config.json` ②`DietRecord` 数据模型 ③`HealthScore` 返回结构。之后各自独立开发 |
| **占位数据解耦** | C 做 UI 时统一用 `FakeRepo` 提供占位数据，**不等 B 的数据库就绪**；B 完成后切换 `RealRepo` 即可 |
| **每日 15 分钟站会** | 只回答三问：昨天完成了什么验收项 / 今天做什么 / 被什么卡住。**卡住超过 2 小时必须喊人**，不许自己硬扛 |
| **不许跨人改代码** | 各自分支开发，D5/D8/D9 三个节点合并。**D10 冻结后任何人不得提交代码** |

### 8.6 依赖关系（关键路径）

```
D0 环境 ──┬─→ D2 Kotlin Mel 前端 ──→ D3 Mel 对齐 ──┐
          │                                          ├─→ D5 端到端闭环 ★CP2
          └─→ D2 Baseline ──→ D3 跨域评估 ★CP1 ─────┤
                                    └─→ D4 TFLite ──┘
                                                      ↓
                              D6 投票机制 ──→ D7 行为分析 ──→ D8 闭环
                                                      ↓
                                          D9 三种 Demo ★CP3 ──→ D10
```

**关键路径 = D0 → D2(Kotlin Mel) → D3(对齐) → D4(TFLite) → D5(闭环) → D9(Demo)**
**任何一环延期，优先砍 D6–D8 的增强功能，绝不动 D5 与 D9。**

---

## 9. 三大 Demo 模式（现场保命方案）

> `讨论1.txt` §6 评估现场失败概率 **30–50%**。**必须做，且必须实测。**

| 模式 | 触发 | 实现 | 技术要点 |
|---|---|---|---|
| **A · 实时识别** | 默认 | 真机麦克风实时采集 + 推理 | 这是主打，成功则全场最佳 |
| **B · 示例音频回放** | 现场失败时点"示例演示" | **内置自采 wav** 走**同一条推理管线**（注入到环形缓冲而非播放文件后录回） | ⚠️ **不要用"播放音箱让麦克风收"的方式** —— 那是二次采集，引入噪声。必须直接注入缓冲 |
| **C · 报告展示** | 兜底 | 预置 7 天历史数据 + 完整报告页 | **数据必须自洽**（见下） |

### 9.1 Demo C 的报告数据（**双轨准备**）

UI 稿显示"已坚持记录 7 天""本周饮食摘要""健康评分 85"—— 这些**不能凭空出现**（`5.png` 的"已坚持 7 天"在 D8 前不可能自然产生）。

**Track 1（首选）：真实累积数据** —— 从 **D6 起，全队每人每天用 App 真实检测 3–4 次**（早/午/晚/零食），持续到 D9 上午，可累积 **3–4 天 × 3 人 × 3–4 次 ≈ 30–45 条真实记录**。

> **为什么这是首选**：真实记录带着**真实的置信度、真实的咀嚼次数、真实的时间分布**。现场评委若追问"这些数据怎么来的"，答案是"我们自己用了 4 天"——这比任何预置数据都有说服力。而且顺带完成了 D8–D9 的实地测试。
>
> **额外收益**：这 4 天的真实使用会**自动暴露大量 UI 与逻辑 bug**（时间显示、跨天分组、统计口径），等于免费做了一轮可用性测试。

**Track 2（兜底）：预置演示数据集** —— 若 Track 1 数据量不足（<20 条），则提供「加载演示数据」按钮（**明确标注为演示数据**），预置：
- 7 天 × 3–4 餐 = 约 25 条记录
- 每条含时间、食物、置信度、咀嚼次数、时长、速度评级
- **评分卡与雷达图数字必须与预置记录算出的结果一致**（消除 §3.8 的 D-1 缺口）
- 周报的"零食次数 ↑20%"等文案与记录数据吻合

**共同验收方式**：写一个单元测试，从**当前生效的数据集**（Track 1 或 Track 2）跑一遍评分引擎，断言输出 == UI 显示的数字。**这个测试很重要 —— 否则演示时点开报告页，数字对不上就尴尬了。**

> ⚠️ **Track 1 的隐私提醒**：团队自用产生的记录属于真实个人饮食数据。若最终提交的 APK 或截图会公开，需确认团队成员同意展示其饮食记录（通常无异议，但**一句口头确认要有**）。

### 9.2 现场执行 SOP

```
1. 提前 30 分钟到场地，实测一次完整流程（环境不同，麦克风底噪会变）
2. 手机开飞行模式（避免来电打断）→ 但确保 App 不需要网络（隐私设计保证）
3. 音量：现场环境噪声 > 65dB 时，直接走 Mode B
4. 准备两台手机（一台备用，型号不同最好，验证泛化）
5. 现场禁止临时改代码（`执行规划草稿.txt` D10 明确要求）
6. 演示脚本按 `讨论4.txt` §10：0-1min 痛点 / 1-5min 核心Demo / 5-8min 健康分析 / 8-10min 技术
```

---

## 10. 答辩抗压包

### 10.1 必答题（Q&A 定稿）

| Q | ❌ 不能说 | ✅ 应该这样答 |
|---|---|---|
| "怎么证明这是 AI，不是声音模板匹配？" | "CNN 学习了声音" | "模型学习的是咀嚼过程的**频谱演化**：瞬态冲击的时频定位、2–8kHz 频段能量分布、以及**时间结构**（咀嚼节律的周期性）。模板匹配对时间偏移和增益变化极其敏感，而我们的模型在增益 ±20% 和 SNR 5dB 下仍保持性能，这证明它学的是**特征而非模板**。" |
| "别人为什么不用？" | "几乎没有" / "我们首创" | "这个方向其实**已经有相当多的工作**，而且近两年很活跃——AutoDietary，以及 2025 年的 ChewSense、NutriEar、AudiNosh。**但它们全部依赖耳戴或专用硬件**：入耳麦克风、耳机反向信号、颈挂式传感器。**共同局限是需要用户额外购买并佩戴设备，日常依从性低。我们的定位是零额外硬件——只用用户本来就有的手机自带麦克风**，用可接受的精度换取零门槛的可及性。"（展示 §3.0.3 对比表） |
| "误识别怎么办？" | "准确率很高" | "系统不是替代专业营养管理，而是降低记录成本。**对于置信度 0.45–0.70 的结果，UI 会主动提示用户确认**（可现场演示）。低于 0.45 则不记录。修正确认的数据会作为反馈样本，用于后续模型迭代。" |
| "健康评分 85 怎么算的？" | （答不上来） | 直接下钻 UI："四项加权 —— 饮食规律性 30 分、食物结构 30 分、零食控制 20 分、进食速度 20 分。**每一项都有明确判据**（展示评分卡）。比如饮食规律性按用户自身三餐时间标准差计算，σ≤30 分钟满分，σ≥90 分钟零分。" |
| "为什么不用摄像头？" | "声音更好" | "摄像头只能识别食物**外观**，而饮食健康不仅关注吃了什么，也关注**如何进食**。声音能提供咀嚼节律、进食速度等动态信息，同时避免拍摄环境带来的隐私问题。两者是互补而非替代。" |
| "手机声音能准确吗？" | "可以" | "我们**没有假设任何环境都能识别**。我们针对移动场景设计，通过**自采环境噪声做增强**，并且**用独立的手机自采测试集**评估跨域性能。这三个数字是分开报告的：公共集 XX%、跨域 XX%、跨人 XX%。"（展示实验矩阵 E1/E2/E3） |
| "数据量够吗？会不会过拟合？" | "够" | "我们同时报告公共集和自采集结果。自采集只有 240 段，**样本量小是事实**，所以我们报告了 **Wilson 95% 置信区间**而不是单点数字，并按**人**划分训练/测试来避免数据泄漏。这也是我们下一步要扩充的方向。" |
| "和普通健康 App 区别？" | "我们更 AI" | "传统 App 依赖用户主动输入，而 AcouDiet 想把健康管理从**主动记录**转向**被动感知**，把成本从'每餐 7 步录入'降到'每次用餐 1 次开关'。" |
| "为什么不做后台常驻、真正无感？" | "还没做" | "这是**刻意的设计权衡**。后台常驻会持续占用麦克风，在功耗和隐私上都不可接受，且系统限制严格。我们选择用户主动发起检测会话 —— 这是隐私、功耗与体验之间的明确取舍。" |
| "（指向命名）**为什么叫 AcouDiet？**" | 支吾 / 说"随便起的" | "**Acou** 取自 Acoustic，**Diet** 是饮食。名字直接点出我们的技术路径——用**声学信息**做饮食管理，而不是靠拍照或手动输入。" |
| "**之前是不是叫 EatSense？**" | 否认 / 支吾 | "是的，我们最初用 EatSense。做文献调研时发现**这个名称与 2023 年爱丁堡大学的一个饮食行为识别数据集重名**，另一个候选名 ChewSense 也已被同类工作占用。为避免检索混淆，我们改成了 AcouDiet。**这次尽调也让我们系统梳理了相关工作，反而帮我们把定位想清楚了。**"（建议 PPT 直接放一页「命名与查重」，见执行包 §5） |

### 10.2 必须在 PPT 里出现的图

1. **相关工作对比表**（§3.0.3）—— "零额外硬件"定位的**证据**，也是把重名危机转成调研充分的关键
2. **源域 vs 目标域对比图**（§3.11）—— 证明团队理解问题本质
3. **三级实验矩阵 E1/E2/E3 结果表**（§3.11）—— 最硬核的技术材料（**数字来自 D3 实测**）
4. **消融实验表**（§3.7）—— 证明每个设计决策都有数据支撑
5. **混淆矩阵**（§7.4）—— 证明评估严谨
6. **评分卡四维下钻截图**（§3.8）—— 证明产品逻辑完整
7. **`aapt dump badging` 无网络权限截图**（§6.1）—— 隐私主张的最强证据
8. **架构图**（`APP期望功能汇总/2.jpg`）—— 定版架构
9. **范围取舍说明页**（§12.5）—— 主动声明 10 天做了什么、没做什么

### 10.3 宣传口径红线（来自 `APP期望功能汇总/1_p3.jpg`，必须遵守）

| ❌ 禁止 | ✅ 改为 |
|---|---|
| "我们可以通过声音准确识别食物" | "我们探索一种新的无感饮食数据采集方式，通过声学信息降低传统饮食记录的操作成本，并进一步结合饮食行为分析形成健康反馈" |
| "识别所有食物" | "识别典型食物类别和饮食行为特征" |
| "记录成本降为零" | "零录入操作；把逐条记录的成本降为一次开关的成本" |
| **"几乎没有相关研究" / "我们首创"** | **"现有工作主要集中在耳戴与专用硬件；我们的定位是零额外硬件的手机端侧方案"**（§3.0.3） |
| **"准确率可达 XX%"** | **"在自采手机测试集上实测为 XX%（含置信区间）"** —— 数字须已测出 |
| **"2 秒内出结果"** | **"开始进食后约 4–5 秒给出确认结果"**（窗口 4.096s + 稳定判据 2s，§3.6） |

**最终 Slogan（材料中的推荐，采纳）**：
> **让记录饮食，从"动手"变成"无感"。**
> 副：听见每一餐，感知每一种习惯。

---

## 11. 风险登记册（含新增风险）

| ID | 风险 | 概率 | 影响 | 应对 | 触发动作 |
|---|---|---|---|---|---|
| R-1 | **NNAPI/GPU 委托不支持 Hardswish → 回退 CPU，延迟不达标** | 高 | 中 | D4 就在真机实测；若不达标则降特征图到 96×96 或提高推理步长到 1.0s | 延迟 > 200ms/帧 → 改参 |
| R-2 | 跨域准确率远低于 in-domain | **高** | **高** | 已用增强 + 自采测试集提前量化；**CP1 降级开关** | CP1 见 §8.4 |
| R-3 | TFLite 转换失败或精度损失 | 中 | 高 | 主路线改 TF 直训（§3.2）+ parity_test 闸门 | parity 不通过 → 重训 |
| R-4 | Kotlin Mel 与 librosa 不一致 | 中 | **高** | D3 强制数值对齐测试 | atol>1e-3 → 立即停手修 |
| R-5 | 现场环境噪声导致识别失败 | 高 | 中 | 三模式 Demo（§9）+ 备用手机 | 现场 >65dB → Mode B |
| R-6 | 10 天工时超支 | 高 | **高** | **3 人版必做 §8.2 范围冻结（释放 3.5 人日）+ D0 并行准备 + 4 个 CP 检查点** | 任意 CP 未过 → 按 §8.2.4 阶梯追加裁剪 |
| R-7 | 志愿者数据采集不到位 | 中 | 高 | D0 即启动；最低保证 3 人（P01-P03 做测试集） | D1 结束仍 <3 人 → 用组内成员自录 |
| R-8 | 命名冲突引发学术诚信质疑 | **高** | 中 | §3.0 更名（**已完成重新尽调**）+ 补相关工作章节 | 立即执行 |
| R-9 | 现场演示时手机来电/系统弹窗 | 中 | 中 | 飞行模式 + 免打扰 + 双机备份 | SOP §9.2 |
| R-10 | 预置 Demo 数据与评分卡数字不一致 | 中 | 中 | 单元测试断言（§9.1） | 测试失败 → 冻结 UI 修复数据 |
| R-11 | 合规：无知情同意书 | 中 | 中 | D0 即签（§6.2） | 未签 → 不使用该志愿者数据 |
| R-12 | C 的 Flutter 技能不足，无法分担 UI | **高** | **高** | D0–D1 完成官方 codelab + 出一个静态页面（§14 P0） | D3 结束仍出不了页面 → 砍报告页趋势图，并把 D6–D8 的 UI 全部降级为静态 |
| **R-13** | **B 单点故障**（唯一懂 Kotlin 原生的人，D2/D5/D9 三个峰值） | **高** | **高** | **Plan-S 兜底（§8.5.1）**：D0 就验证 Task Library 可行性；接口先行解耦 | D2 结束未产出 128×128 数组 → **立即切 Plan-S**，禁止继续自写 Mel |
| **R-14** | **范围蔓延**：中期想"再加个功能" | **高** | 中 | §8.2 裁剪清单**签字确认**并写入仓库 README；任何新增功能须等额删除一项 | 有人提新增 → 先问"删哪一项" |
| **R-15** | 接口未冻结导致 A/B/C 互相阻塞 | 中 | 高 | D1 结束前冻结三个接口（§8.5.2）；C 用 `FakeRepo` 占位 | D2 仍未冻结 → 当天站会强制拍板 |
| **R-16** | **领域已有工作密集**：ChewSense / NutriEar / AudiNosh（均 2025）做了**同一件事** | **高** | 中 | **转为优势**：差异化定位为"**零额外硬件 / 仅手机麦克风**"，5 篇写进 Related Work（§3.0.3） | 被问"别人做过吗" → 主动展示对比表 |
| **R-17** | 材料中"几乎没有研究""首创"等表述**被打脸** | 中 | 中 | 全部材料禁用绝对化表述，统一口径见 §10.3 | 出现绝对化表述 → 立即改写 |
| **R-18** | **命名二次踩雷**（v3.0 推荐的 ChewSense 已被竞品占用） | 中 | 低 | §3.0.4 已重新尽调；**选定前须在 Scholar/ACM/IEEE/GitHub/应用商店五处复核** | 候选名出现在任一库 → 换下一个 |
| **R-19** | **UI 假数据返工**：C 照抄设计稿数字，D8 接通真数据时发现字段无来源 | **高** | 中 | **D1 冻结 UI 数据契约表（§3.4.1）**；表外字段一律不得上 UI | D3 发现 UI 出现契约表外字段 → 当场删除 |
| **R-20** | **参数变更未传播**（改了 `feature_config` 但文档/代码别处仍是旧值） | 中 | **高** | §4.1.1 变更单逐项打勾；交付前全局搜索 `3s`/`hop.*160` 命中须为 0 | 搜索有命中 → 立即修正并复查 |
| **R-21** | **Demo 首次结果耗时超预期**（4.096s 窗口 + 2s 稳定判据 ≈ 4–5s），现场显得"卡" | 中 | 中 | 改 Demo 脚本：先放波形动画 + 未确认预测，4–5s 后出确认结果（§3.6 UI 提示） | 现场有人质疑慢 → 解释窗口长度原理，并展示"未确认实时预测"通道 |

---

## 12. 交付物清单（3 人版 · 已按 §8.2 裁剪）

### 12.1 软件（✅ 交付）
- [ ] Android APK（release 签名，`minSdk 24`）
- [ ] **特征规格文档 `feature_config.json`**
- [ ] 完整 Flutter 源码（含 Kotlin 原生模块）
- [ ] **4 个页面**：首页 / AI 检测 / 饮食记录 / 健康报告

### 12.2 AI（✅ 交付）
- [ ] `model_int8.tflite`（≤2.5 MB）
- [ ] `model_fp32.tflite`（对照用）
- [ ] 训练脚本 + 增强脚本 + 评估脚本
- [ ] **`parity_test.py` 及通过的输出日志**
- [ ] `metrics.json`：全指标 + Wilson CI + 混淆矩阵
- [ ] `confusion_matrix.png`
- [ ] **消融实验报告**（降噪 on/off + 增强组合，简化版）
- [ ] **三级实验矩阵结果**（in-domain / 跨域 / 跨人）
- [ ] 自采数据集（240 段 + 标注 + 划分文件）

### 12.3 文档（✅ 交付）
- [ ] 项目计划书（**已更名 / 已加相关工作章节**）
- [ ] 技术方案文档（含 §4 冻结规格）
- [ ] 测试报告（全指标 + CI + 混淆矩阵 + 消融）
- [ ] 答辩 QA 手册（§10.1）
- [ ] **伦理与知情同意书归档**

### 12.4 演示（✅ 交付）
- [ ] PPT（≥7 张关键图，§10.2）
- [ ] 演示视频（2 分钟备份，`讨论2.txt` 要求）
- [ ] **三模式 Demo 实测通过记录**
- [ ] 现场 SOP 卡片（§9.2）
- [ ] 预置演示数据集（Mode C 兜底）

### 12.5 ❌ 明确不交付（3 人版裁剪，写进 PPT"后续工作"）

> **重要**：这一节要**主动写进答辩 PPT**，而不是等评委问。主动声明"我们在 10 天内做了明确的范围取舍"是成熟团队的标志；被问出来才是失分。

| 不交付项 | 归属阶段 |
|---|---|
| 登录 / 账号体系 | 第二阶段 |
| iOS 版本（架构兼容，未验证） | 第二阶段 |
| CSV 导出 | v1.1 |
| 手动修正（仅保留二选一确认） | v1.1 |
| 「我的成就」解锁系统（仅静态展示） | v1.1 |
| 检测页「识别历史」列表 | v1.1 |
| 咀嚼节律标准差 σ | v1.1 |
| RIR 混响 / Mixup 增强 | 第二阶段 |
| 蓝牙耳机 / 智能手表采集 | 第三阶段 |
| 多模态融合（视觉 + IMU） | 第三阶段 |
| LLM 健康助手 | 第三阶段 |
| 15–20 类食物扩展 | 第二阶段 |

### 12.6 📄 必须修改的既有材料（不是新写，是**改**）

> 这一节的用途：把散落在全文的修正**收敛成一份可执行的改稿清单**。每一项都指向已有文件。

| 文件 | 需要的修改 | 依据 | 状态 |
|---|---|---|---|
| `计划书 1版.docx` | ①更名 ✅ ②§5.1.1/§6.3.2/§6.3.3 三处参数（见 §4.1.1 变更单）③§4.1 功能表按 §8.2 裁剪 ④新增 Related Work 章节 ⑤§7.1 参数量 2.5M→**实测值** | §3.0、§3.1、§8.2 | ①✅ 其余待做 |
| `计划书 2版.docx` | 同 1 版（两版技术章节逐字相同，**可批量替换**） | 同上 | ①✅ 其余待做 |
| `讨论1~4.txt`、`执行规划草稿.txt`、`导师汇报部分参考.txt` | 更名（共 15 处） | §3.0.4 | ✅ **已完成** |
| `讨论4.txt` | §5 双输出头设计标记为**已否决** | §1.2 定版架构 | 待做 |
| `执行规划草稿.txt` | ①D1 `duration=3s` → `4.096` ②§10 "2 秒内显示结果" → **4–5 秒**（§3.6） | §4.1.1 | 待做 |
| `软件UI界面设计图/1.png` | ①Logo 更名 ②雷达图四轴、能量区间、食材多样性 → 按 §3.4.2 | §3.4 | 待做（D1） |
| `软件UI界面设计图/2.png` | ①Logo 更名 ②缩略图限 6 类、"近 7 天"→"本周" | §3.4.2 | 待做（D1） |
| `软件UI界面设计图/4.png`,`5.png` | **作废归档**，以 8/9/10 新版为准 | N-12 | 待做 |
| `软件UI界面设计图/8,9,10.png` | ①Logo 更名 ②条目命名、动态计数、导出置灰 → 按 §3.4.2 | §3.4 | 待做（D1） |
| `软件UI界面设计图/6~10.png` | **删除重复副本**（与 1~5 同一文件） | §1.1 N-1 | 待做 |
| 全部材料 | 删除"几乎没有""首创""识别所有食物""成本降为零" | §10.3 | 待做 |

> **已完成**：更名全量执行（7 份 txt + 2 份 docx 副本 + 本方案），详见 §3.0.4。
> **剩余**：参数修正、UI 稿改造、文案红线清理 —— 均随 D1 起步一并处理。

---

## 13. 与原始材料的差异汇总（可追溯）

| # | 原方案 | 修正为 | 依据 |
|---|---|---|---|
| 1 | 音频窗 3s | **4.096s** | §3.1 数学自洽 |
| 2 | PyTorch→ONNX→TFLite | **TF/Keras 直训→TFLite** | §3.2 转换风险 |
| 3 | Dart 算 Mel | **Kotlin 原生算 Mel** | §3.3 性能+精度 |
| 4 | 双输出头（食物+属性） | **单头 + 知识库查表** | 定版架构 + `建议汇总`(1) |
| 5 | 谱减法降噪（既定） | **可开关实验变量** | §3.7 |
| 6 | 测试集随机划分 | **按人 + 按会话划分** | `建议汇总`(4) |
| 7 | 10 帧众数投票 | **三级机制（EMA+稳定判据+人机协同）** | §3.6 |
| 8 | 无评分算法 | **四维 100 分评分卡** | `建议汇总`(2) |
| 9 | "成本降为零" | **"零录入操作"** | §3.9 |
| 10 | Hive / SQLite 混用 | **统一 SQLite** | N-13 |
| 11 | 蓝牙耳机采集 | **降级为扩展功能**（PPT 写"后续可扩展"） | `建议汇总`(3) |
| 12 | 1000 条自采片段 | **240 段（可解释、可复现）** | N-14 |
| 13 | 无 D0 | **新增 D0 并行准备日** | §2.1 环境实测 |
| 14 | 进食结束=30s 静音 | **90s 静音** | N-6 |
| 15 | iOS + Android 双交付 | **Android 单平台** | C-1 |
| 16 | 5 页面含登录 | **4 页面（砍登录）** | N-10 |
| 17 | 项目名 EatSense | **更名（ChewSense 亦不可用）** | §3.0 重新尽调 |
| 18 | 无相关工作 | **补 Related Work + 领域工作对比表** | §3.0.3 / §3.0.5 |
| **19** | **9 模块 + 5 页面全交付** | **裁剪 7 项功能（§8.2.2），释放 3.5 人日** | **3 人 × 10 天工时约束** |
| **20** | **B 一人承担原生+UI+集成** | **C 从 D3 起分担 3 个页面 UI** | **消除 B 的单点瓶颈** |
| **21** | **无技能兜底方案** | **新增 Plan-S（Task Library 替代自写 Mel）** | **B 是全队唯一不可替代环节** |
| **22** | 修正只写在"差异表"里 | **新增 §4.1.1 参数变更传播表 + §12.6 材料改稿清单** | **修正必须落到文件，否则等于没改** |
| **23** | 无 UI 数据来源规范 | **新增 §3.4.1 UI 数据契约表（D1 冻结）** | **防止 C 照抄假数据导致 D8 返工** |
| **24** | — | **删除 v3.0 自造的准确率预测值，改为 D3 实测 + Wilson CI** | **不得用假精度掩盖真不确定性** |
| **25** | — | **CP1 判据改为"现场 10 次实拍成功率 ≥6/10"** | **判据须直接对应项目成败且可当场测量** |

---

## 14. 立即行动清单（3 人版 · 未来 48 小时）

| 优先级 | 动作 | 负责 | 交付 |
|---|---|---|---|
| **P0** | ~~决定项目更名~~ → **✅ 已定：AcouDiet / 声膳** | 全员 | — |
| **P0** | ~~命名 5 处人工复核~~ → **✅ 自动化已完成**；剩 4 项人工确认（见 `AcouDiet_命名查重验证记录.md` §3） | C | 查重记录 |
| **P0** | ~~全局替换~~ → **✅ 已完成**（7 txt + 2 docx 副本 + 本方案） | B+C | §3.0.4 |
| **P0** | **⚠️ 手工删除两个只读 docx 原件**（内部仍含旧名，无法自动覆写） | C | 目录清理 |
| **P0** | **确认 §8.2 范围冻结清单**（7 项裁剪签字） | 全员 | 签字单已备好 |
| **P0** | **执行 §4.1.1 参数变更传播表**（9 项逐项打勾） | A+B | 打勾完成的变更单；全局搜索 `3s`/`hop.*160` 命中为 0 |
| **P0** | **冻结 §3.4.1 UI 数据契约表** | 全员（C 主笔） | 契约表 + 冻结确认 |
| **P0** | ~~环境安装（AI + Flutter/Android SDK）~~ → **✅ 已完成**（PASS=28/FAIL=0，含 GPU 实测） | — | `_toolchain\环境安装报告.md` |
| **P0** | **沙箱外确认 `flutter doctor -v`**（沙箱内 piped stdio 被禁，无法验证） | B | Android toolchain 全绿截图 |
| **P0** | **确认 `feature_config.json` 的 `n_frames`（128 vs 129）** → 见环境报告 §6 | A | 选项 A/B 拍板 |
| **P0** | 启动志愿者招募 + 打印同意书 | C | ≥3 人签字 |
| **P0** | **B 技能预演**：Kotlin 录 **4.096s** PCM 并打印 RMS | B | 可运行的 hello-audio demo |
| **P0** | **Plan-S 可行性调研**：Task Library `AudioClassifier` 能否直接接入 | B | 结论（可行/不可行）+ 最小 demo |
| **P0** | **C 技能预演**：Flutter 官方 codelab 跑通 + 出一个静态页面 | C | 能 `flutter run` 出页面 |
| P1 | 下载 Eating Sound Collection，筛选 6 类 | A | 类别分布表 |
| P1 | 采购/确认演示食物（薯片、苹果、饼干、面包、胡萝卜、饮料） | C | 实物 |
| P1 | 录制自采环境噪声 3 场景 | C | 3 个 wav |
| P1 | 冻结三个接口：`feature_config` / `DietRecord` / `HealthScore` | A+B+C | 接口文档（§8.5.2） |
| P2 | 建立仓库骨架 + 分支策略 | B | Git 仓库 |
| P2 | 准备答辩 QA 初稿（§10.1） | C | 文档 |
| **P1** | **补 Related Work 章节**（§3.0.5 提纲，5 篇必引） | C（A 校技术表述） | 章节初稿 |

---

## 附录 A：材料中的"优质建议"已采纳清单

以下来自讨论文件、已全部纳入本方案，标注**采纳**以示可追溯：

| 来源 | 建议 | 状态 |
|---|---|---|
| `讨论1.txt` §3 | 增加环境扰动增强模拟手机环境 | ✅ §7.3 |
| `讨论1.txt` §6 | 三级 Demo 降级 | ✅ §9 |
| `讨论1.txt` §7 Q3 | 低置信度提示用户确认 | ✅ §3.6 Level 3 |
| `讨论2.txt` §5 | 8 类降至 6 类 | ✅ §4.3 |
| `讨论2.txt` §13 | 咀嚼行为分析（比识别 20 类更像健康产品） | ✅ §5.4 |
| `讨论3.txt` | 类别难度分级、明确技术边界 | ✅ §4.3 + §3.4 |
| `讨论4.txt` §6 | 3 人自采 + 3 场景 | ✅ §7.1（规模务实化） |
| `执行规划草稿.txt` §6 | Demo 三模式 | ✅ §9 |
| `执行规划草稿.txt` §9 | 风险降级路径 | ✅ §4.3 + §8.4 CP1 |
| `导师汇报部分参考.txt` §9 | 4 个待验证问题 | ✅ 转为 D0–D3 的验证任务 |
| `建议汇总`(1) | 声音+时间双模态 | 🔶 部分采纳：时间特征进规则引擎；模型融合列为后续工作 |
| `建议汇总`(2)1 | 属性不做第二输出头 | ✅ §1.2 定版架构 |
| `建议汇总`(2)2 | 评分算法必须透明 | ✅ §3.8 |
| `建议汇总`(2)3 | 蓝牙耳机降级 | ✅ §13 #11 |
| `建议汇总`(2)4 | 自采数据按人划分 | ✅ §7.2 |
| `建议汇总`(2)5 | 报告 P/R/F1/混淆矩阵 | ✅ §7.4 |
| `APP期望功能汇总/1_p3` | 宣传口径红线 | ✅ §10.3 |

## 附录 B：未采纳项及理由

| 材料建议 | 未采纳理由 |
|---|---|
| `建议汇总`(1) 声音+时间**模型级**融合 | 需改模型结构、重训、重转换，10 天内投入产出比低。**时间特征已进规则引擎**（夜间进食、零食时段判定），达到同等产品效果。列为第二阶段 |
| `建议汇总`(2)4 "约 1000 条片段" | 3 人无法产出可解释的 1000 段；改为 240 段真实可复现数据（N-14） |
| `讨论2.txt` "Flutter TFLite 插件，但 Android 原生更稳" | 采用 Flutter 框架（已向导师承诺），但同时引入 **Kotlin 原生 Mel 前端**，兼顾开发效率与性能 |
| docx §15 风险1 "必要时降低特征图到 96×96" | 保留为 R-1 的应对手段，但**不作为首选** —— 改分辨率会导致与训练不一致，须重训 |
| `讨论4.txt` §5 双任务头 | 见 N-2，与定版架构冲突 |

---

**文档结束**

---

## 变更记录

| 版本 | 变更 |
|---|---|
| v3.0 | 首版。材料清点、13 项漏洞评审、冻结规格、倒排计划 |
| v3.1 | **团队确认为 3 人**。新增 §8.2 范围冻结（7 项裁剪，释放 3.5 人日）；§8.5 重排为 3 人分工并把 3 个页面 UI 转给 C；新增 CP4 与 Plan-S 技能兜底；§9.1 报告数据改双轨；§12.5 明确不交付清单；风险表新增 R-13/R-14/R-15 |
| **v3.2** | **把"修正清单"变成"全文贯穿"**。①§3.0 更名重新尽调 —— **推翻 v3.0 推荐的 ChewSense（已被竞品占用）**，新增 5 篇领域工作对比与 Related Work 提纲 ②新增 **§4.1.1 参数变更传播表**（9 项打勾单），并修正 v3.1 自身遗漏的 §8.3/§14 残留"3 秒" ③新增 **§3.4.1 UI 数据契约表** + §3.4.2 视觉稿逐张修正清单 ④**删除 v3.0 自造的准确率预测值**（85–95%/55–80%），CP1 判据改为"现场 10 次实拍成功率" ⑤§3.6 统一"帧 vs patch"术语并补 UI 响应时间说明 ⑥新增 §12.6 既有材料改稿清单、R-16～R-21 |
| **v3.3** | **✅ 更名拍板：`AcouDiet` / 声膳**。§3.0.4 更新为决策记录（含查重结果、被否决候选留档、答辩话术）；配套产出独立文档 **`AcouDiet_更名执行包.md`**（五处人工复核清单 + 54 项全局替换清单 + Related Work 成稿 + PPT 备用页模板 + 执行顺序）；§12.6 与 §14 同步更新 |
| **v3.4** | **更名全量执行完毕**（2026-09-10）。①7 份 txt 替换 15 处 ②2 份 docx 生成已更名副本（各 23 段，**并顺带修复了原文件内嵌图片的 CRC 损坏**）③本方案修正 3 处被误判为历史语境的残留（架构图标题、答辩话术、§12.6 表）④新增 `AcouDiet_命名查重验证记录.md`（四路实测零命中 + 2 个近似项风险评估）⑤新增 `_toolchain\环境安装报告.md`（PASS=28/FAIL=0，RTX 5060 GPU 实测通过）⑥发现并记录 `n_frames` 应为 129 而非 128 |

> **下一步**：仅剩 **§3.0 的更名决策**一项待定，它直接决定全部材料（docx、UI 稿、PPT、APK 包名）的改动范围。
> 建议顺序：**先拍板更名 → 再执行 §14 的 P0 清单**（其中"§8.2 范围冻结签字"和"B/C 技能预演"是 3 人版能否成立的两个前提，必须先做）。
