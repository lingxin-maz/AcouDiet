ADR-36: put the project under version control (first commit)

Why: this tree had NO version control until now, and the ADR log records
irreversible losses caused by that; ADR-34's on-device-language-model removal
had to be hand-archived before anything could be deleted.

Root is the workspace root, not AcouDiet/: the authoritative material is spread
across three siblings -- shared/ (the SSOT feature_config.json), docs/ (the
frozen SPEC tree + the ADR log) and AcouDiet/ (the code). A repo rooted at
AcouDiet/ would not version the constitution.

Also lands this session's fixes:
  * the 4 widget tests broken by ADR-30 are fixed, and the real flutter test
    (137 tests) is green and wired into tool/verify_all.ps1
  * ADR-35: the TFLite AAR is swapped for 16 KB-page-aligned LiteRT, plus the
    new gate tool/check_page_alignment.py
  * ADR-33/34: checkers moved into the repo, stale criteria fixed
  * the on-device language model is removed (ADR-34)

Ignored: build output, toolchains, APKs, the 511 MB training checkpoint.
