# 模型选型调研：日志分析 + 个性化方案（v0.1 草稿）

| 项 | 值 |
|---|---|
| 日期 | 2026 年调研，v0.1 草稿 |
| 性质 | **调研草稿，不是权威文档**。不是 SPEC / 不是 PLAN，**不得作为任务来源**。结论须经人工拍板后写入 `docs/01_裁定记录ADR.md` 与 `API-05 §9` 才能实施。 |
| 起因 | 用户提出「引入新模型分析 App 日志与每次记录，为用户个性化输出方案」，并给出候选 `smolify/smolified-nutriai-distilled-nutritionist` |
| 上游约束 | `common/docs_api/API-05_后端数据通讯规范.md` §9/§10、`common/PLAN-00`、`backend/.../A-02_规则引擎健康建议/SPEC-A-02` §1.3 |

> ⚠️ **取证说明**：本次调研所在环境的网络出口受限（huggingface.co / hf-mirror / r.jina.ai 等域名的 DNS 解析被拦截），**我无法打开任何模型卡、论文或项目页面**，只有搜索索引层的信息（标题、URL、摘要片段）。因此下文中凡标 **【待核对】** 的条目，落笔前必须由人打开原始页面逐项确认，**不得直接采信**。

---

## 0. 三句话结论

1. **这不只是「选哪个模型」的问题，而是「选哪条架构路线」的问题**，因为 AcouDiet v1.0 的三条宪法级约束（无网络权限、LLM 状态 `DISABLED`、分析平面逐字可复现）会把绝大多数候选方案直接判死。
2. **`smolified-nutriai-distilled-nutritionist` 可以看，但不能作为选型依据**：蒸馏来源不明、无公开营养基准成绩、几乎肯定是英文语料，对一个要落 Android、要应对答辩追问的研究型项目来说，证据链太薄。
3. **推荐路线是「分层」而不是「换模型」**：本地确定性统计 + 规则（已有 A-02）→ 结构化「证据包」→ 小模型做**排序/选择**（而非自由生成）→ 免责声明恒在末位。云端 LLM 只作为 v2.0 的可选润色层，且需先改 ADR。

---

## 1. 先厘清任务：你说的「日志分析」与「记录分析」是两件不同的事

把两件事混在一个模型里，是这类需求最常见的翻车点。

| | 任务 A：App 运行日志分析 | 任务 B：饮食记录分析 → 个性化方案 |
|---|---|---|
| 数据 | 埋点事件、崩溃栈、耗时、状态机迁移 | `diet_record` 类别/时间/置信度/`behavior_metrics` |
| 目标 | **开发者**看的：定位缺陷、funnel、性能回归 | **用户**看的：这周该怎么调整 |
| 正确武器 | 聚合统计 + 异常检测（确定性），**不需要 LLM** | 规则/评分（已有 A-01/A-02）+ 可选小模型排序 |
| LLM 的角色 | 只把已算好的指标**翻译成人话**（可选、可后置） | 只在「自由生成方案」时才需要（高风险） |
| 隐私 | 日志含设备信息/路径，**绝不能整包外传** | 结构化记录，隐私等级低一档（但仍属个人信息） |

> **建议**：任务 A 用「本地结构化日志 → 本地聚合器 → 固定模板摘要」，模型都不用引入；把预算和风险集中到任务 B。如果日志分析的目标其实是**给导师/答辩展示"AI 在分析"**，那么 A 用确定性统计反而更容易讲清楚、更容易过复现性条款。

---

## 2. 硬约束：先看这道墙，再看模型

这是本调研最重要的一节。**下列约束不是建议，是已签字的裁定**：

| # | 约束 | 出处 | 对新模型意味着什么 |
|---|---|---|---|
| C1 | v1.0 **没有云端后端，且不允许有**；App **无 INTERNET 权限**是可验证的宣传证据 | `API-05 §1`、`§10`；`C-01_权限最小化与无网络权限可验证` | 任何需要联网推理/拉模型的方案，**必须走 ADR 修改** |
| C2 | 引入 LLM 健康助手被列为云端协议触发条件 **T1**，当前状态 **`DISABLED`，无实现** | `API-05 §9.1`、`§9` 阅读须知 | 「引入 LLM」= 触发 v2.0 设计，**必须同步重写隐私主张** |
| C3 | 分析平面必须**可复现**（同输入 100 次输出逐字相同；无随机、无时钟、无 IO、无网络）——文档自称"答辩的保命条款" | `API-05 §6.1`；`SPEC-A-02 §2.2/2.3` | **自回归 LLM 天然违反此条**（采样随机、版本漂移） → 必须改 ADR 或把 LLM 输出降级为「非权威装饰层」 |
| C4 | 建议域**不得新增 `dimension` 枚举值**；免责声明恒存在恒在末位；文案冻结 | `SPEC-A-02 §1.2/§1.3` | LLM 输出必须是**受限槽位填充**，不能自由造维度 |
| C5 | 制品交付需三个 hash 闭环；模型按版本化文件名投放 | `API-05 §7`、`§7.1` | 新模型必须走同一交付闸门 |
| C6 | 当前 release APK ≈ **25.5 MB** | 交付记录 | **打包 1–3 GB 的端侧 LLM 直接不可行** |

### C6 的推论（关键洞察）

「端侧 LLM」听起来最符合隐私路线，但它有一道很多人忽略的坎：

- **要么打进 APK** → 体积从 25 MB 涨到 GB 级，应用商店与答辩演示都不可接受；
- **要么运行时下载权重** → **需要 INTERNET 权限** → 直接摧毁 C1 的核心证据（"APK 连联网权限都没有"）。

也就是说，**「端侧 LLM」与「无网络权限」在当前形态下是互斥的**，除非本项目愿意把"首次联网下载模型"写进隐私主张。这个取舍必须先于选型拍板。

---

## 3. 候选模型盘点（含核实状态）

### 3.1 端侧小模型（若选 Route B）

| 候选 | 规模 | 许可 | 优势 | 风险 | 核实 |
|---|---|---|---|---|---|
| **Gemma 3n E2B / E4B** | 有效参数 ~2B/4B | Gemma Terms | **原生多模态含音频输入**，与"声学感知"卖点天然咬合；Google 官方端侧栈支持 | 非商用友好许可条款需读；内存占用按 GB 计 | 多来源称其"可在 ~2GB RAM 上离线运行、支持文本/图像/**音频**/视频"【待核对原文】 |
| **Gemma 3 1B-IT** | 1B | Gemma Terms | 体积最小档，LiteRT / MediaPipe 官方支持 | 1B 的中文与推理能力有限，方案质量存疑 | 存在 HF 仓库【待核对】 |
| **Qwen3 系列 0.6B / 1.7B / 4B** | 0.6–4B | Apache-2.0 | **中文最强梯队**，许可最干净，GGUF/LiteRT 生态成熟 | 4B 端侧延迟与发热需实测 | 【待核对量化版本与端侧跑分】 |
| **Phi-4-mini-instruct** | 3.8B | MIT | 许可干净，推理能力强于同规模 | 中文一般 | 【待核对】 |
| **SmolLM3-3B** | 3B | Apache-2.0 | 许可干净，社区活跃 | 中文一般 | 【待核对】 |
| **MedGemma 1.5 4B（含 LiteRT 版本）** | 4B | Health AI 条款 | 医学知识底子好，已有端侧导出 | **是医学模型不是营养模型**；医疗合规与免责风险反而更高 | 存在 `*-LiteRT` 仓库【待核对】 |

**端侧运行时选型**（同样影响可行性）：LiteRT-LM（Google，原 TFLite 线，Android 官方）、MediaPipe LLM Inference API、llama.cpp / GGUF、ONNX Runtime GenAI、Qualcomm GenIE（NPU）。
- Google AI Edge Gallery 已支持音频，LiteRT-LM Android 有 Maven 制品【待核对版本号】。
- 有实测博客称「Llama 3.2 3B + KV cache INT4 量化可压到 ~2GB 运行于 Android」【待核对：这是单篇博客，非官方基线】。

### 3.2 云端 API（若走 Route C）

DeepSeek / Qwen(DashScope) / GLM / Kimi / GPT / Gemini 均可胜任「把结构化证据包写成自然语言方案」。
**优势**：零端侧体积、质量高、可随时升级。
**代价**：触发 C1+C2——必须改 ADR、改宣传口径、加账号体系与端到端加密（`API-05 §9.2 D3–D5`）。

### 3.3 领域模型与工具（无论走哪条路线都值得用）

| 名称 | 类型 | 为什么值得关注 | 核实 |
|---|---|---|---|
| **NutriBench** | 评测数据集（ICLR 2025） | 专门评测 LLM 从餐食描述估算营养（碳水/热量，MAE/RMSE）。**这是你答辩最需要的"我们有基准"证据** | 出现在 ICLR 2025 proceedings【待核对指标定义】 |
| **Taralli** | 方法论（prompt 优化 + 模型选择） | 针对"营养卡路里估算"的迭代式 prompt 优化与选型流程，可复用其评测框架 | ZenML LLMOps DB 收录【待核对】 |
| **KG-DietNet** | 论文（知识图谱 + LLM 膳食推荐） | 用 KG 约束 LLM 输出，正好对应 C4「不许自由生成」的思路 | IEEE 收录【待核对】 |
| **Medi-LLM** | GitHub 项目 | 边缘设备多智能体做饮食规划的工程参考 | 开源仓库【待核对可用性】 |
| **FoodyLLM** | HF 小模型 | 食物域小模型，可作对照基线 | 仓库存在，卡片未读【待核对】 |
| **Qwen3-4B-medical.gguf** | 社区微调 | 医学域中文小模型（已 GGUF 量化），可做对照 | 仓库存在【待核对训练数据与评测】 |

### 3.4 你提到的 `smolify/smolified-nutriai-distilled-nutritionist` 尽调

**已确认（搜索索引层）**
- 模型确实存在于 Hugging Face：`smolify/smolified-nutriai-distilled-nutritionist`
- README 标注 **license: apache-2.0**

**未能确认（我打不开页面，且这些恰恰是选型的关键信息）**
- 基座模型是什么？参数量多少？
- 蒸馏的**教师模型**是谁？蒸馏数据从哪来、是否含受版权/许可限制的语料？
- 有没有**任何量化评测**（尤其在营养/膳食建议任务上的准确率、幻觉率）？
- 是否支持中文？训练语料语言分布？
- 是否做过安全性对齐（对"疾病诊断/用药"的拒答能力）？

**我的判断（属于意见，不是事实）**
- 命名 `distilled-...-nutritionist` + `apache-2.0` 的组合，通常意味着「某基座 + 少量营养域蒸馏数据 LoRA」，这类模型的**公开可复现性普遍较弱**，且 `apache-2.0` 只覆盖它自己发布的权重，**不能覆盖基座与训练数据的许可风险**。
- 用在**医疗相邻**场景（膳食建议），一个来源不明的蒸馏模型在答辩上几乎无法自证安全性；而被问「你的营养建议依据是什么」时，规则引擎反而能一条条指出来。
- **结论：可以作为 Route B 的"候选之一"进入横评，但不建议作为主选**。真想用，先跑三件事：① 打开模型卡核实上述 6 项；② 与 Qwen3-1.7B/4B、Gemma 3 1B 做同 prompt 横评；③ 用 NutriBench 或自建金标集量化。

---

## 4. 四条路线对比

| | **A 纯确定性（加强版规则/轻量 TFLite 排序器）** | **B 端侧 LLM** | **C 云端 LLM** | **D 分层混合（推荐）** |
|---|---|---|---|---|
| 是否需改 ADR | **否**（继续用离线制品闸门） | 需改：C3 复现性条款 | 需改：C1+C2+宣传口径+账号体系 | 需改：仅"可选润色层"部分 |
| 联网权限 | 无 | **权重下载则需联网**（见 C6） | 必需 | 核心链路无 |
| APK 体积 | +几 MB | +0.3–3 GB 或走下载 | +几乎为 0 | 同 A |
| 输出可复现 | ✅ 逐字可复现 | ❌ 需固定 seed/温度 0，仍难保证 | ❌ | ✅ 主体可复现 |
| 个性化能力 | 中（排序/分档） | 高（自然语言） | 最高 | 高（在受控范围内） |
| 幻觉/医疗风险 | 极低 | 中高 | 中高 | 低（LLM 只润色已确定的结论） |
| 答辩可辩护性 | **最强** | 弱（"为什么不用云端"+"为什么敢离线让 2B 模型给健康建议"） | 中（但要正面回答隐私代价） | 强 |
| 工程工作量 | 小 | 大 | 中 | 中 |

### Route D 的具体形态（建议）

```
[已有] 录音 → TFLite 分类 → diet_record + behavior_metrics
                                    │
[已有] A-01 健康评分卡 ──┐           │
[已有] D-03 周聚合    ──┼─→ HealthScore + WeekSummary
                        │
[新增] 特征化：把 HealthScore/WeekSummary/近 7 天记录编码成
       固定长度、整数化、版本化的「证据包 evidence_pack」
                        │
[新增] 小模型/规则做「方案选择与排序」：从冻结的候选方案池里选 + 排序
       （输出 = 结构化 slot，不是自由文本；不新增 dimension 枚举）
                        │
[已有] A-02 规则引擎文本模板填充 → Advice[]
                        │
[可选 v2.0] 云端 LLM 只把上述结构化结果「润色成人话」，作为装饰层，
            失败即降级回模板；绝不参与事实判断
```

关键设计点：
- **让模型做选择题，不要做问答题**。方案池是有限的、人工审过的、可举证的；模型只在池内做排序与个性化权重。
- **输出结构固定**，从而保住 C3 的复现性（固定权重 + 整型量化 → 逐字可复现）。
- **免责声明与"数据不足不编造"仍由 A-02 兜底**（`SPEC-A-02 §2.2` 第 1、2、7 步）。

---

## 5. 评测方案（选型前必须先定标准）

没有评测就选模型 = 不可答辩。建议按顺序做：

1. **自建金标集**：从已有 demo 数据（`A-04 演示数据双轨`）里取 50–100 个"窗口 → 期望方案"样本，由**人（导师/有营养背景的同学）签字**。
2. **指标**：
   - 方案可执行性（人评 1–5）
   - 覆盖率（数据不足时是否老实说"暂不生成"——`SPEC-A-02` 已定义该行为）
   - **幻觉率**（是否出现了证据包中没有的数字/事实）
   - 越界率（是否越出 5 个 `dimension`、是否做疾病诊断）
   - 端侧：首 token 延迟、峰值内存、耗电、体积
   - 可复现性：同输入 100 次是否逐字相同（**这是硬门槛，不是软指标**）
3. **外部基准**：用 **NutriBench**（ICLR 2025）做横向对照，即使只报"我们在该基准的抽取子集上的表现"，也远胜"我们用了 XX 模型"。
4. **横评矩阵**：`smolified-nutriai-distilled-nutritionist` vs `Qwen3-1.7B/4B` vs `Gemma 3 1B` vs 「纯规则基线」。**纯规则基线必须参赛**——大概率它会赢，而这本身就是一个很有说服力的答辩结论。

---

## 6. 需要人工拍板的问题（阻塞项）

| # | 问题 | 影响 |
|---|---|---|
| Q1 | 是否允许 v1.0 引入联网能力（即是否修改 C1/C2）？ | 决定 Route B/C 是否还存在 |
| Q2 | 若允许，宣传口径如何改？"音频永不上传 + 仅结构化记录加密同步"是否可接受？ | `API-05 §10` 已给出建议口径 |
| Q3 | 分析平面"逐字可复现"是否可以让位给 LLM？还是必须保留确定性主线？ | 决定 Route D 是否成立 |
| Q4 | 新的分析功能是 v1.0 交付项，还是"后续工作"？ | 影响排期与 `PLAN-00` |
| Q5 | 是否接受"人工审过的候选方案池 + 模型做选择题"这一形态？ | Route D 的前提 |

---

## 7. 附：本次调研的证据等级声明

| 等级 | 含义 | 本文中出现的内容 |
|---|---|---|
| ✅ 已核实 | 可在本仓库文档中逐字查到 | 全部硬约束（§2 C1–C6） |
| 🟡 索引层 | 搜索引擎确认存在该页面/仓库，但**我未能打开原文** | 所有候选模型与其规模/许可 |
| 🟠 单源未复核 | 仅一篇博客/一个仓库的说法 | "3B 模型 2GB 可跑"类性能声明 |
| 🔴 观点 | 我的判断，非事实 | §3.4 对 smolify 模型的评价、§4 路线对比打分 |

**下一动作建议**：先回答 §6 的 Q1/Q3/Q4，这三个答案会直接砍掉一半候选，剩下的再进 §5 的横评。

---

## 8. 追加：Route D 已选（2026 调研，人工确认）

> 决策者已选择 **Route D 分层混合**。本节把它落成可执行形态。
> 仍然**不是权威文档**，需转写为 `SPEC-A-05` + `PLAN-A-05` + ADR 后才可实施。

### 8.1 Route D 的一个重要澄清

**Route D 的「选择与排序」那一步，根本不需要 LLM。**

这是好消息：它把「引入新模型」从"要不要引入大模型"降级成"要不要引入一个几百 KB 的排序器"。
一个表格化的个性化排序任务（用户档案 × 周模式 → 候选方案打分），用 LLM 是**杀鸡用牛刀且引入不可复现性**。

| 层 | 推荐技术 | 规模 | 可复现 | 网络 |
|---|---|---|---|---|
| L0 感知（已有） | TFLite 音频分类 | 已有 | ✅ | 无 |
| L1 评分/聚合（已有） | 确定性公式 + SQL 聚合 | — | ✅ | 无 |
| L2 **证据包编码（新增）** | 纯 Dart/Kotlin 确定性函数 | 0 | ✅ | 无 |
| L3 **方案选择与排序（新增）** | **首选：INT8 TFLite 前馈网络 / GBDT**；备选：纯加权规则 | 几十 KB ~ 几百 KB | ✅（整型） | 无 |
| L4 文案填充（已有 A-02） | 冻结模板 | 0 | ✅ | 无 |
| L5 润色（可选，v2.0） | 云端 LLM | — | ❌ 装饰层 | 需要 |

### 8.2 L3 的候选技术（替代"大模型"）

| 候选 | 何时选它 | 备注 |
|---|---|---|
| **纯加权规则打分** | 首选基线。候选池 ≤ 20 条时几乎够用 | **必须参赛**。若它不输给任何模型，就是答辩最强结论 |
| **GBDT（sklearn/LightGBM → 手工转 Kotlin 或 m2cgen）** | 特征 ≤ 50 维、样本量几百~几千 | 推理快、无浮点随机性；导出后需数值对齐测试 |
| **小型 MLP → INT8 TFLite** | 需要非线性交互（如 年龄 × 晚餐时间） | **复用现有 `T-07 INT8 量化与 TFLite 导出` + `T-08 数值对齐` 工具链**，这是本项目最大的复用红利 |
| **Learning-to-Rank（LambdaMART）** | 若"从候选池选出 top-k"是核心任务 | 与任务形式天然匹配 |
| **Qwen3-0.6B/1.7B LiteRT** | 只有当需要**理解用户自由文本**时才考虑 | 体积 0.4–1.2 GB，与 C6 冲突 → 应列为 v1.1+ 后续工作 |

### 8.3 必须新增的契约（否则不可复现也不可交付）

1. **`evidence_pack` schema（版本化，仿 `C-03 feature_config` 单一真源）**
   固定字段顺序、固定单位与舍入（沿用 `API-05 §6.2`）、固定编码版本号。
2. **候选方案池（frozen candidate pool）**
   人工审过的 N 条方案，每条带 `planId` / `applicableDimensions`（**不得超出 5 个已有枚举**）/ `evidenceSlots`（只允许引用证据包中存在的字段）。
3. **交付闸门**：排序器模型走 `API-05 §7.1` 三 hash 闭环；工作目录建议同时产出 `evidence_pack.schema.json` 与 `plan_pool.json` 的 hash。
4. **降级语义**：L3 缺失/加载失败 → 回退到加权规则基线；**任何情况下不得静默产出空方案**（沿用 `SPEC-A-02 §2.2` 第 1、2 步的"general 恒存在 + 数据不足不编造"）。

### 8.4 需要写的 ADR（三条）

| 拟定编号 | 内容 | 为什么必须写 |
|---|---|---|
| ADR-26 | 分析平面分层：L4 及以下**保持逐字可复现**；L5 润色层明确排除在复现性承诺之外 | `API-05 §6.1` 自称"答辩的保命条款"，不改就违反 |
| ADR-27 | **C4 豁免的边界**：允许在候选池内做排序，但**不允许新增 `dimension` 枚举值、不允许模型自由生成方案文本** | `SPEC-A-02 §1.3` |
| ADR-28 | 新增功能编号与范围：数据不足、免责声明、空态的行为沿用 A-02，不重新定义 | 防止与 `SPEC-A-02` 产生口径冲突 |

> **编号说明**：`docs/01_裁定记录ADR.md` 当前已排到 **ADR-25**（ADR-19 已被 FF-19 类别表修订占用），故本调研的条目从 **ADR-26** 起编号。

> **顺带提醒**：`SPEC-A-02 §1.3` 第 33 行现在写的是"**不接入 LLM 或任何网络服务**"。
> Route D 落地时这句话**必须显式修订**（而不是被忽略），否则实现方按现行规范写代码会直接与需求冲突——这正是 `docs/01_裁定记录ADR.md` 存在的意义。

### 8.5 Route D 的下一步动作清单

| # | 动作 | 产出 | 阻塞依赖 |
|---|---|---|---|
| 1 | 起草 ADR-26~30 提请拍板 | ADR 条目 | 无 |
| 2 | 定义 `evidence_pack` 字段清单（从 `HealthScore` + `WeekSummary` + `D-04 用户档案` 里取） | `evidence_pack.schema.json` | 动作 1 |
| 3 | 编写冻结候选方案池（建议 12–20 条，人工审） | `plan_pool.json` | 动作 1 |
| 4 | 建 50–100 条金标集（窗口 → 期望方案），人工签字 | `plan_gold.csv` | 动作 2、3 |
| 5 | **先实现加权规则基线**并测指标，再决定是否值得上模型 | 基线报告 | 动作 4 |
| 6 | 若基线不足：训练排序器 → 走 `T-07`/`T-08` 导出与对齐 | `.tflite` + hash | 动作 5 |
| 7 | 端侧日志分析（任务 A）：本地聚合 + 固定模板，不引入模型 | 日志摘要模块 | 独立，可并行 |

**关键判断**：动作 5 是分水岭。**如果加权规则基线已经够好，正确答案就是"不需要新模型"**——而这个结论本身就值一篇答辩陈述。

---

## 9. 追加：建议浮窗（报告更新 → 分析 → 弹窗）设计

### 9.0 已由决策者确认的两项

| 项 | 选择 | 含义 |
|---|---|---|
| 浮窗形态 | **应用内浮层**（只在 AcouDiet 界面内弹出） | 零新权限；`SPEC-C-01 §7 判据 #2`「权限集合 == `{RECORD_AUDIO}`」继续成立 |
| 触发时机 | **两个来源都要**（新记录落库 / 报告页刷新），各自节流，同一天同窗口只弹一次 | 需要一套统一的闸门，而不是两套 if |

### 9.1 形态必须冻结在「应用内」——这不是审美选择，是可判定的红线

| 实现方式 | 是否需要新权限 | 对 `SPEC-C-01 §7 判据 #2` | 判定 |
|---|---|---|---|
| Flutter `Overlay` / `Stack` + `Positioned` / `showDialog` / `showModalBottomSheet` | 否 | 权限集合仍是 `{RECORD_AUDIO}` | ✅ **采用** |
| `SnackBar`（项目主题已是 `SnackBarBehavior.floating`） | 否 | 同上 | ✅ 可作降级形态 |
| **系统级悬浮窗** `SYSTEM_ALERT_WINDOW`（"显示在其他应用上层"） | 是（敏感权限） | **判据 #2 直接不通过** | ❌ **禁止** |
| 前台服务 + 通知（`FOREGROUND_SERVICE` / `POST_NOTIFICATIONS`） | 是 | **判据 #2 直接不通过** | ❌ **禁止** |

**注意 `C-01` 的措辞是"集合等于 `{RECORD_AUDIO}`"**，不是"包含 `RECORD_AUDIO`"——所以哪怕只多加一个权限，这条最强隐私证据就作废（`API-05 §10` 已把代价写清楚）。且 `_toolchain/check_apk_contents.py` 现在会对 release 包做 `--expect-no-internet` 检查，任何引入网络栈的浮窗 SDK 都会当场翻红。

### 9.2 「报告更新」目前**不是**一个事件，必须先定义

现有代码里，报告数据只在 `ReportNotifier.reload()` 里被读取（`lib/presentation/state/notifiers.dart:227`），而 `reloadFresh()` 由 `AppShell._refreshTab()` 在**每次切 Tab 时**调用（`app_shell.dart:85`，ADR-23）。
**结论：绝不能把"reload 完成"直接当作弹窗触发** —— 否则用户每切一次 Tab 就弹一次，第 4 次就会想卸载。

建议把触发定义成一个**纯计算的门控函数**，两个来源都收敛到它：

```
       源 α：记录落库成功（检测页保存 / demo 注入）
                    │
                    ├──────────────► PlanPopupGate.evaluate(snapshot)
                    │                        │
       源 β：ReportNotifier.reload() 完成    │  返回 PlanPopupDecision
                    │                        │  （show / suppress + reason）
                    └────────────────────────┘
                                             │
                              AcouDiet 应用内浮层宿主（AppShell）
```

- **源 α** 是"内容真的变了"的强信号；
- **源 β** 是"用户主动来看"的触达信号；
- 两者共用同一套闸门与同一本账本，因此**无论从哪来，同一天同窗口都只弹一次**。

### 9.3 五道闸门（全过才弹，任一不过就静默）

| # | 闸门 | 判据 | 为什么必须有 |
|---|---|---|---|
| G1 | **数据充足** | `WeekSummary.recordCount >= A-02-K4` 阈值 | 继承 `SPEC-A-02 §2.2` 第 2 步「数据不足不编造」。**此时不弹，比弹一条免责声明更正确** |
| G2 | **有实质建议** | 非 `general` 的 `Advice` 至少 1 条 | 否则弹的就是纯免责声明，纯打扰 |
| G3 | **会话静默** | `DetectNotifier` 无进行中检测会话；**且** FF-24 首次隐私弹窗已关闭 | `M-01` 现场 SOP 不能被弹窗打断；启动首帧不能和隐私弹窗撞车 |
| G4 | **节流** | 账本中不存在 `(localDay, windowKind)` | 你选定的"同一天同窗口只弹一次" |
| G5 | **无重复浮层** | 当前没有已打开的建议浮层 | 防止源 α/β 同时命中导致叠两层 |

> G3 是本设计里最容易被漏掉、后果最严重的一条：**在录音检测过程中弹窗，现场演示会当场出事故。**

### 9.4 浮窗里显示什么：一个字都**不新生成**

- 内容 = `AdviceEngine`（`A-02`）已经产出的 `Advice[]` 中**优先级最高的 1–3 条非 `general` 项**，**原文照搬**；
- 末位**必须**带 `A-02-K6` 的冻结免责声明（"提供日常健康管理建议，不进行疾病诊断，不替代专业医疗意见"），**不因空间不足而省略**；
- 一个主按钮「查看报告」→ 切到 `ShellTab.report`（复用 `AppShell._select`，**不要 `Navigator.push`**，否则会改变 ADR-23/24 依赖的路由与 Tab 假设）；
- **新模型的全部职责 = 决定"从池子里挑哪 1–3 条、按什么序"**。这就是 Route D 的 L3，而且它完全可以是一个纯函数。

### 9.5 落地骨架（对齐现有目录与纪律）

| 动作 | 文件 | 说明 |
|---|---|---|
| 新增 | `app/lib/domain/service/plan_selector.dart` | **纯 Dart 纯函数**：`(HealthScore, WeekSummary, ProfileView, List<Advice>) → PlanSelection`。无 IO、无时钟、无随机、无依赖 —— 与 `advice_engine.dart` 同级同风格 |
| 新增 | `app/lib/domain/service/plan_popup_gate.dart` | 五道闸门的判定，同样纯函数 + 注入的账本端口（便于单测） |
| 新增 | `app/lib/presentation/widgets/advice_popup.dart` | 浮层组件（`Stack`/`Positioned` 或 `Overlay`） |
| 新增 | `app/lib/presentation/state/` 内挂一个 `PlanPopupNotifier` | 与现有 `AcouNotifier` / `AcouScope` 体系一致（**不要引入 Riverpod**，`pubspec.yaml` 的"依赖极简"是刻意选择，见其注释） |
| 改动 | `app/lib/presentation/pages/shell/app_shell.dart` | 在 `Scaffold` 外包一层浮层宿主；仍是 4 个 Tab，**不得因此加第 5 个**（FF-23 / ADR-12 冻结） |
| 改动 | `app/lib/presentation/state/notifiers.dart` | `ReportNotifier.reload()` 成功后投递源 β 信号（**只投递，不直接弹**） |
| 改动 | `app/lib/presentation/state/app_services.dart` | 注册新服务（该文件是 L2 port 的唯一装配点） |

**重要**：这个新模型**不需要 TFLite、不需要 Kotlin、不需要 MethodChannel**。
它是 Dart 层的排序逻辑，体积为 0，因此**不触碰 `API-05 §7` 的模型交付闸门，也不加重 APK**。

### 9.6 节流账本放哪

| 方案 | 做法 | 代价 |
|---|---|---|
| **首选（轻）** | 复用 `D-04 本地用户档案与设置项`，存两个键：`planPopupLastDayKey` / `planPopupLastWindowKind` | 不需要数据库迁移；只能记"最近一次" |
| 备选（重） | `D-01` 新增表 `plan_popup_ledger(windowKey TEXT, windowKind TEXT, shownAtMs INTEGER, PRIMARY KEY(windowKey, windowKind))` | 触碰 `D-01` schema + `D-02` 迁移，需走变更传播单 |

按"同一天同窗口只弹一次"的语义，**首选方案就够**——没必要为它开一张表。

### 9.7 测试防线（按本仓"把事故钉住"的既有风格）

新增 `app/test/ui/advice_popup_test.dart`，至少 5 条：

1. 检测会话进行中触发 → **不弹**（G3）；
2. 同一 `(day, windowKind)` 第二次触发 → **不弹**（G4）；
3. `recordCount` 不足 → **不弹**（G1）；
4. 弹窗展示的文案与顺序 == `AdviceEngine` 输出中前 N 条非 `general` 项，且**免责声明恒在末位**（G2 + `A-02-K6`）；
5. 弹窗出现/关闭后，`AppShell` 的 `IndexedStack` 各页状态与 Tab 索引不变（防止浮层宿主改坏 ADR-23/24 的行为）。

再加一条纯域测试：`plan_selector` 同输入 **100 次输出逐字相同**（对齐 `API-05 §6.1` 的复现性条款）。

> 这正好补上 ADR-24 记录的那个教训：「**没有任何一个测试 pump 过 `AppShell` 本身**」。浮窗宿主恰恰是加在 `AppShell` 上的——第 5 条测试就是为它准备的。

### 9.8 追加两条 ADR

| 拟定编号 | 内容 |
|---|---|
| **ADR-29** | 建议浮窗形态**冻结为应用内浮层**；**禁止**为它引入 `SYSTEM_ALERT_WINDOW`、`FOREGROUND_SERVICE`、`POST_NOTIFICATIONS` 或任何新权限，以维持 `SPEC-C-01 §7 判据 #2`（权限集合 == `{RECORD_AUDIO}`） |
| **ADR-30** | 弹窗触发与节流语义：两个来源收敛到同一闸门；同本地日同窗口最多一次；检测会话中与 FF-24 隐私弹窗未关闭时静默；内容一律取自 `A-02` 的 `Advice[]`，**不新生成文本** |

### 9.9 与 `SPEC-A-02 §1.3` 的冲突（必须正面处理）

`SPEC-A-02 §1.3` 现在写着「**不接入 LLM 或任何网络服务**」。
如果最终路线是**纯 Dart 确定性排序器**（本设计的推荐形态），那么：

- **不接 LLM，也不接网络** → 这句话**仍然成立**，无需修订；
- 但 §1.3 同时写着本域「不做个性化饮食处方」——这一句**必须修订**，否则实现方照规范写代码会与需求直接冲突。

也就是说：**Route D 的最优形态，恰好可以只改一句话就通过。**

---

## 10. 模型推荐清单（最终，含 `smolified-...` 判定）

### 10.1 判定：`smolify/smolified-nutriai-distilled-nutritionist` 可行吗

**结论：作为 v1.0 落地方案 —— 不可行；作为 v1.1+「后续工作」里的对照基线 —— 可以。**

**（A）项目约束层：与它本身好坏无关的否决理由**

即便它是世界上最好的营养模型，它是 **LLM**，因此同时撞上三道墙：

| 撞上的约束 | 后果 |
|---|---|
| `API-05 §9.1 T1`（引入 LLM = v2.0 云端协议触发条件） | 必须改 ADR + 改写隐私主张口径 |
| `API-05 §6.1`（分析平面同输入 100 次逐字相同） | 自回归生成天然不满足 |
| `SPEC-C-01 §7 判据 #2`（权限集合 == `{RECORD_AUDIO}`）+ 当前 APK 仅 25.5 MB | 要么联网拉权重 → 加权限；要么打包 GB 级权重 → APK 爆炸 |

**（B）证据层：我实际探查到的（2026 调研，取证受限）**

| 已确认 | 说明 |
|---|---|
| 仓库确实存在 | `smolify/smolified-nutriai-distilled-nutritionist`，另有 modelhub 镜像 |
| `license: apache-2.0` | 见模型卡 raw 链接 |
| README 首行 / 组织标语为 **"Smolify: Intelligence Distilled"** | 说明定位是"蒸馏出的小模型" |
| 仓库含 **`chat_template.jinja`** | 是 HuggingFace transformers 格式的 **instruct/chat** 模型 |
| **搜索不到任何量化版（GGUF）、任何第三方评测、任何独立引用** | 只有原始仓库 + 镜像，无社区量化、无下游项目 |

| **未能确认（=选型关键全部空白）** |
|---|
| 基座模型 / 参数量 |
| 蒸馏的**教师模型**是谁 |
| 蒸馏数据来源与许可 |
| 任何营养任务的量化成绩（准确率 / 幻觉率） |
| 中文支持 |
| 安全性对齐（对"疾病诊断/用药"的拒答能力） |

> 取证受限说明：本机 DNS 对 `huggingface.co`、`hf-mirror.com`、`dev.modelhub.org.cn`、`r.jina.ai` 均返回非公网 IP（`web_fetch` 拒绝）；`cn.bing.com` 可直接抓取，但搜该模型名返回的**全是无关结果**。故上表"未能确认"是**真的没查到**，不是没查。

**（C）答辩风险层（最致命）**

膳食建议属准医疗场景。被问「**你的营养建议依据是什么**」时：
- 规则引擎（`A-02`）可以逐条指向 SPEC 与冻结文案；
- 一个**教师模型不明、训练语料不明、无任何评测**的蒸馏模型，**答不上来**。

### 10.2 推荐：按"位置"给，而不是给一个模型

**关键认知：你的架构里有 3 个不同位置，它们要的东西完全不同，不要用同一个东西填。**

| 位置 | 任务 | **推荐** | 体积 | 理由 |
|---|---|---|---|---|
| **L3 方案选择与排序**（本次真正要引入的） | 从冻结候选池挑 top-1~3 并排序 | **① 加权规则打分器（首选）→ ② LightGBM / 小型 MLP(INT8 TFLite)** | **0 ~ 几百 KB** | 可复现、零权限、零网络；MLP 路线直接复用 `T-07 INT8 量化` + `T-08 数值对齐` |
| **L5 润色**（可选，v2.0，`DISABLED`） | 把结构化结论写成自然语言 | **DeepSeek API** 或 **Qwen-Plus / GLM** | 云端 | 中文质量好、成本低；且它**只润色不判断** |
| **端侧 LLM**（仅当将来要做语音/文本咨询，v1.1+） | 自由对话 | **Qwen3-1.7B / Qwen3-4B**（Apache-2.0、中文最强、LiteRT/GGUF 生态成熟）<br>**Gemma 3n E2B**（原生音频输入，与声学卖点咬合）<br>**Gemma 3 1B**（最小档） | 0.4–2 GB | 这三个的可核验性都**远超** smolify 那个 |

**明确不推荐**：`MedGemma` 等**医学**模型用于本场景——它是医学不是营养，且会**提高**医疗合规与免责风险，对一个强调"不进行疾病诊断"的产品是反向操作。

**评测工具**：`NutriBench`（ICLR 2025）作为外部基准；自建 50–100 条人工签字金标集作为主判据。

### 10.3 一句话回答

> **换模型解决不了这个问题，换任务分解才能。**
> `smolified-nutriai-distilled-nutritionist` 不可行，**不是因为它差，而是因为你的项目要的不是 LLM**。同理，换成 Qwen3 或 Gemma 3n 也一样不可行——约束是同一个。

### 10.4 若仍想给它一次机会：最低成本验证路径

1. 打开模型卡，核对 §10.1(B) 的 6 项空白；
2. 本地用 llama.cpp / transformers 跑 50 条金标（输入 = `evidence_pack`，输出 = 从候选池选 1–3 条）；
3. 与**加权规则基线**对比幻觉率、越界率、可复现性、延迟、体积。

若它赢不过基线（大概率），**这个"输"本身就是答辩素材**：「我们评测了 N 个候选模型，最终选择了可复现的确定性方案」——比"我们用了 XX 模型"强得多。
